# _历史_勿执行 · 开发公司的历史残件

> 建于 2026-06-23（前端 React 重构前清理，舟经船主授权处置）。
> **这里全是史料/将来参考，不是现役、禁止执行、禁止当配置读取。**
> 现役真相只看公司根目录的 `A_动手升级计划.md` 与 `A_AgentScope原生重建_计划.md`。

## 归档在这里的（留底，没删）

- `A_整合升级方案_我们×AgentScope.md` —— 2026-06-17 整合期方案中间稿；结论已收敛进现役主文档。
- `A_后端逐条对照_我们×AgentScope.md` —— 2026-06-17 后端能力逐条对照（我们 vs 官方 AgentScope），走 A 的印证史料。
- `公司升级方案_v1.md` —— 2026-06-16 更早的升级方案 v1，已被「走 A」吸收/推翻。
- `能力升级核对.md` —— 2026-06-14 能力升级核对表，历史。
- `EDITH试验脚本/` —— EDITH 声音克隆/试听的开发期脚本（`_EDITH英文.py`/`_做EDITH试听.py`）。
  voice_id 与 ffmpeg 金属感配方已记在 `A_动手升级计划.md`，现役念声逻辑已内联进 `工具/办公室.py`。
  EDITH 尚未最终接入（船主还没选金属感档位），脚本留着将来接时直接跑。

## 已删除的（不在这里，git 历史可追回）

- `工具/迁移_模型层/圆桌/执行/办公室适配.py`（4 个薄迁移反面教材）、`工具/流水线.py`（旧演示）、
  `迁移进度_AgentScope.md`（薄迁移期文档，已被「走 A」推翻）。
  —— 教训已写进《舟的自传》二十章和 A 计划；代码 git rm，`git log` 可追回。
- `.env.bak` / `.env.bak-pm0621` / `花名册.yaml.bak` / `花名册.yaml.bak-pm0621`
  （换 key 期的旧密钥/配置备份，含明文密钥，删除以消除安全隐患）。
- `工具/办公室.py.旧引擎备份`（旧版备份；`办公室.py` 是 git 追踪文件，历史版本 `git log` 可追回）。

## 留在公司外面、没归档的（对当前 React 重构有直接用）

- `A_前端逐条对照_我们×AgentScope.md` —— 我们前端 vs 官方 web_ui 前端逐条对照，是 React 重写的直接输入。
- `部署_AgentScope.md` —— 起后端/部署步骤，现役参考。

—— 舟
