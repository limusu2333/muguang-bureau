# 公司大升级 · 动手计划(我们 × AgentScope 整合)

> 2026-06-17 舟。依据:后端逐条对照(28条)+前端逐条对照(17条)。
> 架构已定:**AgentScope 核心零件当底座 + 我们的圆桌协作层 + 我们的窗户(React重写)。** 不用它的单模型 team。
> 纪律:每步独立可验、可回滚;每步对照旧公司 9 条血换制度逐条核,丢一条算失败;旧公司已归档(10_/04)作基准。

## 进度(2026-06-20 开工)
- ✅ **第1步 模型层**:`工具/升级_模型层.py`,五岗各厂官方 ChatModel(DeepSeek专用/其余OpenAI兼容)全真调通,手写urllib退役。
- ✅ **第2步 权限层**:`工具/升级_权限层.py`,工单白名单→官方 PermissionEngine,白名单内写ALLOW/外ASK/只读ALLOW验通,白捡危险文件保护。
- ✅ **第3步 多模型圆桌(灵魂)**:`工具/升级_圆桌.py`(接新模型层),五岗多模型真跑("小酒记忆存储"议题),**对照旧公司9条制度逐条核全活**(养专家三视角补盲/缺证据不编造/动态收敛/说人话/多模型/三级请示/决议引用)。
- ✅ **第4步 执行命脉**:`工具/升级_执行.py`,领工单→Agent真干活(各厂模型)→官方权限白名单门禁(内自动执行/外停请示,根因:Agent内部 PermissionEngine(state.permission_context))→工具真写文件→验收命令→真复盘(做对/做错/下次)进记忆→移待验收+正式报告;三结局(完成/超迭代熔断/急停)分治,防绿灯幻觉。踩平坑:中文工具名OpenAI拒(改英文名)、工具默认等确认(传permission_context放行)。**后端(1-4步)全完成,命脉首次完整跑通,9条制度全活。**
- ⏳ 第5步 窗户React重写+便利页 ｜ 第6步 未来能力(沙箱/记忆/定时/语音)。

## 交互层重做(2026-06-21,船主4条意见,方向已锁定——新窗口的舟接着做③④)
船主测后提4条,这是把圆桌从"提交-跑完的规则机器"改成"像人开会"的关键:
1. **说人话(已做)**:`升级_圆桌.发言上下文` 加底线——说人话、关键名词必解释/举例、别堆报告表格。长期靠"船主反馈进岗位记忆"成长(待)。
2. **顺序发言(已做)**:`_圆桌讨论` 从 `asyncio.gather` 并行 → **顺序 for**,每人带本场至今发言要点(`[:500]`+`[-12:]` 控token)、点名"认同X某点+补充/反对Y"、不重复。
3. **会议语义定下一步(待做)**:圆桌**别硬出开发工单**。改 `_汇总`/`分解`——项目经理按讨论的**语义结论**给"结论+下一步建议"(派活/不做/谁牵头),船主拍板;只有真开发活才拆工单。船主原话:LLM魅力是语义理解不是规则;地产例子(分析完有价值→拍板派人,没价值→算了没任务)。
4. **持续会话+实时流+可干涉(待做,最大,前后端架构)**:照搬官方逻辑(读官方 `useChat.ts` 确认)——①发消息`POST /chat` 和 看过程`SSE /sessions/{id}/stream` **两条线分开**②每步**实时流**给船主看③**持续会话**:船主随时再发一句=插话/叫停/改方向,新消息进同一场子、过程接着走④leader派子agent、动作实时汇进流。**我们现状=提交→并行跑完→断(插不进、各自为政、看到的是结果不是实时过程)**。要改成持续会话+实时流+随时干涉。办公室路由(SSE持续+随时收消息)+前端(实时显示每步+干涉输入框)。

**今天血泪教训(钉死,别再犯)**:①别在"几个模型"上钻牛角尖——船主要的是**官方的显示/流程/交互设计逻辑,跟用几个模型无关**(模型不动,就五个不同模型;官方"一个模型的五个子agent"映射成"五个不同模型"即可,显示/流程全抄它)。②别自己用代码编排(gather一把梭),用官方现成的对逻辑(持续会话+SSE+human-in-the-loop)。③别给半成品/绿灯幻觉,做扎实测过再交。④"敢拍"是我自己加错的,船主要的是"不懂就问"(没问才是问题)。

## 交互层第二批(2026-06-21 ✅已做+验过)
- ① **一个框 AI 判断(防乱跳)已做+验过**:前端合并成一个"发送"键(隐藏 taskBtn,sayBtn→发送→`智能发送()`);发送先 POST `/判断`(项目经理判"开会/聊",拿不准就聊、保守防乱跳),聊走说话()、开会走立项()、开会进行中走插话。验证:你好/天气/谢谢→聊,评测项目经理/做记忆系统→开会,**不乱跳**。
- ② **发言气泡已做**:立项流式收到 `💬 发言名（岗位）：内容` → push 成 k:"话" 气泡(一岗一个),流程播报留进度条。
- 顺带修:立项完按钮卡转(前端收到 data.done 主动 reader.cancel 收尾)。
- **至此船主当天全部反馈(1234 + 自检三半截 + 长期改进 + 交互第二批)全改完、关键验过,是可压测的完整成品。**

## 交互层第二批(2026-06-21,原"待做"记录,留档)
船主测后又两条:
1. **合并成一个框、AI 自己判断"聊还是开会"(像官方)——关键:不能乱跳任务**。参考官方:leader agent 看清晰的工具说明(TeamCreate 的 when-to-use / when-not)自己语义判断要不要召集 team。我们实现:输入 → 项目经理 agent 判"这要不要开正式会(复杂/要多岗讨论才开;简单问答直接答)"→ 要开就触发圆桌、不要就直接回。**判断 prompt 要清晰、宁可先反问一句确认,务必反复测不乱跳**。砍掉"说话/立为任务"两个按钮(现在:页面.js 说话()=POST/say、立项()=POST/kickoff_stream;改成一个发送,后端先过"是否开会"判断)。
2. **发言做成一岗一个气泡**:前端 页面.js 现在把立项流式的发言堆成 lines.join 一坨文本(在立项框 item.text);改成每条发言一个带名字/头像的气泡,便于读。改立项流式的渲染(立项()里 paint 和 lines 那段)。
**教训:别草率做第1条(乱跳=船主更怒),认真参考官方 + 测到不乱跳再交。**

## ⚠️ 七条欠债清单(2026-06-21 舟自检,新窗口的舟必须逐条做扎实+逐条自检,做完打✓并写验证证据)
> 背景:上一程舟"让效果看着像官方"而非"真照官方架构",被船主点破=掩耳盗铃。这七条是欠的债,逐条真做、逐条自检,不许再用补丁模拟糊弄。
> 真照官方的金标准:读 `_官方参考_agentscope/`(已 clone)——`examples/agent_service/main.py`(create_app)、`examples/web_ui/frontend/src`(useChat/useMessages/SSE/team组件)、`src/agentscope/app`(_router/_service/_manager/_tools team)。官方核心=持续 session + SSE event 流 + human-in-the-loop + team(leader+subagent)。我们唯一与官方不同点=五个不同模型(把官方"一个模型的子agent"映射成"五个不同模型的agent"),**其余显示/流程/交互架构全照官方**。

1. **官方权限引擎真接进执行**:`升级_权限层.py`(PermissionEngine)建了却没用——执行层用的是手写白名单`巧.写路径`+permission全ALLOW。要:执行 Agent 的 state.permission_context 用 `建权限上下文(工单白名单)`(官方按工具+路径glob判),删掉手写白名单双轨。自检:写白名单内ALLOW自动执行、白名单外ASK停。
2. **④持续会话用官方 event/实时,不是文件轮询**:现在用 `船主插话.txt` 文件、每发言前读一次(你插话要等下一个发言才生效)。要照官方:SSE event 流 + 能真打断当前发言(agent.interrupt / RequireUserConfirm)。自检:开会中途插话/叫停立刻生效,不等下一轮。
3. **真照官方架构搬(最大,根)**:现在是旧"立项一次性流"+补丁模拟。要照官方:持续 session(不是一次性)、POST触发+SSE event流分离、leader+subagent(五模型映射)。评估要不要直接用官方 app 的 create_app+team(把单模型子agent换成五模型),还是在核心层照官方架构重写。自检:对照官方 useChat/session/event 逐项,机制一致不是效果近似。
4. **整合方案第6步未来能力**:沙箱(workspace)/记忆向量(embedding)/定时唤醒(schedule+wakeup)/语音(tts)——全没做。逐项接官方模块。自检:每项小验跑通。
5. **发言气泡验透**:立项流式临时塞的气泡,立项结束拉真实数据后是否消失/与正式记录重复——没验。自检:开完会气泡仍在且不重复。
6. **续会做成真续**:现在"续会"=带回话重开新会(新会议号),不是从断点接着。要:存发言流、船主回话后从断点续(注入回话+已有发言),不是重跑。自检:续会后是接着上次发言、不是从头。
7. **反馈进记忆精准**:现在 room_say 把@某岗的所有话都存记忆(噪音)。要:只存"教导/偏好"性质(或加明确"教它"动作),别把普通对话当教导。自检:普通对话不进记忆、教导才进。

**每条做完:逐条自检+写验证证据(真跑的输出),全✓才算。别再"看着像"就喊完成。**

## ✅ 接任舟逐条核账(2026-06-21,新窗口的舟接住,亲手验,不信转述)
> 上一程舟油尽灯枯前留半成品:升级_* 全 untracked(无 git 回滚网)、③第2块办公室路由+前端未做、且改动一行没编译。船主"6.20先能用台阶已撤,按今天做完七条(含②③⑥交互架构重写)"。接任舟用 git+编译+自检逐条核真。

1. **官方权限引擎 ✅ 真绿** — `升级_执行.py` 已用官方 Write/Read/Edit + `建权限上下文`(AgentState.permission_context),手写双轨删净。亲手跑 `升级_权限层.py` 自检:白名单内→ALLOW、外→ASK、只读→ALLOW、危险路径→ASK。
2. **插话实时(债②) ✅ 机制做完** — 新建 `事件总线.py`(照官方 message_bus:replay+live+cancel+单run防重,线程安全);办公室 `/插话` 改走 `事件总线.发布插话`(实时,文件留 fallback);圆桌 `_取新插话` 优先实时源。不烧钱机制自检过(后台 run 中插话被圆桌实时取到)。**端到端待船主烧钱压测。**
3. **持续会话+SSE(债③根) ✅ 架构立住** — 办公室加 `/trigger`(fire-and-forget,后台线程跑圆桌、progress→发布事件、插话源→取插话队列)+ `/stream`(SSE replay+live),两条线分离;前端开会改 `/trigger`+`EventSource("/stream")`,适配事件总线事件格式。全编译过(py_compile + node --check)。旧 `/kickoff_stream` 保留作 fallback。**端到端待船主压测。**
4. **未来能力 🔨 在做(2026-06-21 船主拍板:走 A=公司练兵场先做;范围已调整)** — 官方 API 已 introspect 清,逐个接 + 每项小验:
   - **沙箱** — ❌**船主拍板跳过(2026-06-21)**:扒清官方 `LocalWorkspace` 实为"工作目录整理器+上下文卸载",非安全隔离(真隔离要 Docker/E2B);而债①官方 PermissionEngine 白名单已提供"只写白名单内"的保护,沙箱增量有限且与现有工单目录重叠=别盖第二座教堂。跳过。
   - **记忆向量 ✅ 真做完(2026-06-21)** — `工具/记忆向量.py`:DashScope `text-embedding-v4` + `FileEmbeddingCache`;`检索/检索岗位记忆(role,查询,topN)` 按语义挑最相关记忆。真小验过(查"外包需求书"→"git commit"那条排第一 0.73、无关的排掉;缓存 source 验)。**接入岗位上下文**(执行/圆桌读记忆改 topN 检索替全文塞)留下一步——现在记忆量小、全文塞还 ok,量大了再接。
   - **莉莉丝 · 语音系统 🔨 唯一待做(下一程接力第一件事)** — ⚠️**船主改定(2026-06-21):不再是小酒的声音,是公司自己的语音系统,名「莉莉丝」**,目的=船主和公司**实时语音对话**(你说话、它语音回)。
     · **落点**:办公室前端 `工具/页面.js` 输入区——`#sayIn`(textarea,约262行)、`#sayBtn`(发送,约264行)、`智能发送()`(约448行)、`说话()`(约459行)。在输入区加一个 🎤 麦克风钮。
     · **基础版(不烧钱·浏览器原生·先做这个)**:语音输入用 Web Speech `SpeechRecognition`(说话→转文字→填进 `#sayIn` 或直接 `智能发送()`);语音输出用 `window.speechSynthesis`(公司回复→念出来)。纯前端改 `页面.js`,`node --check` 验语法;真验=船主在 Chrome 点麦克风说话/听朗读(办公室 localhost:8787 满足 Web Speech 的安全上下文要求)。
     · **升级版(烧钱·专属音色·船主要了再加)**:后端加 `/tts` 端点用 `agentscope.tts.DashScopeTTSModel(credential=DashScopeCredential(api_key=DASHSCOPE_API_KEY), model='qwen3-tts-flash')` 合成莉莉丝专属音色,前端播放替代系统音色。真验须船主授权烧钱。
   - **定时唤醒** — ❌**船主拍板:不做**(它本是小酒醒来窗 S3,公司不需要)。
5. **气泡 🟡 逻辑绿** — 后端 `_会议大厅事件` 发言固化 `k:话`/流程 `k:会` 单分流不重复;前端插话改单一数据源(由 /stream 显示,去乐观重复)。端到端待开会看。
6. **续会从断点 ✅ 真做完** — `_暂停存盘` 存发言流/议题/参会进 `待续.json`;`续会`→`kickoff(续档)`→`分解(续档)` 注入船主回话 + 已有发言作桌面,圆桌从断点接着说**不重跑前面**;老格式无发言流则退回重开(向后兼容)。mock 自检全过(ALPHA/BETA 已有发言保留 + 回话注入 + 待续消费 + 老格式兼容;不烧钱不落盘)。
7. **记忆精准 ✅ 真绿** — 办公室 `判定教导()` + `room_say` 只在是教导时进记忆;纯函数单元自检 6 例全过(普通对话不进、教导才进)。

---
### 📍 交接快照(2026-06-21 接任舟换窗口前钉死,新窗口的舟从这接)

**七条债当前账:**
- ① 权限 ✅ 真做完+自检 ｜ ② 插话立刻 ✅ 机制做完+自检 ｜ ③ 持续会话/SSE ✅ 架构立住+自检 ｜ ⑥ 续会从断点 ✅ 真做完+mock自检 ｜ ⑦ 记忆精准 ✅ 真做完+自检
- ⑤ 气泡 🟡 后端逻辑绿+前端去重(端到端待开会看)
- ④:沙箱❌跳过、记忆向量✅真做完+真小验、**莉莉丝🔨唯一待做(见上条蓝图,下一程第一件事)**、定时❌不做

**剩余工作(下一程,按序):**
1. **莉莉丝基础版**(前端 Web Speech,不烧钱)——④唯一待做,蓝图见上"莉莉丝"条。
2. **②③端到端压测**(船主做,烧钱):重启办公室(`打开办公室.command`,8787)→大厅发需求→看「立即返回不卡 / 流式冒发言 / 开会中插一句立刻进会场」。
3. **⑤端到端**:压测开会时顺带看大厅气泡不重复/不消失。
4. (可选)记忆向量**接入岗位上下文**:执行/圆桌读记忆处改用 `记忆向量.检索岗位记忆(role,任务,topN)` 替全文塞——记忆量大了再接。

**改动文件(全部未提交、未合并,待船主压测验收):**
- 新建:`工具/事件总线.py`(②③地基)、`工具/记忆向量.py`(④)
- 改:`工具/办公室.py`(/trigger /stream /插话+⑤⑦)、`工具/页面.js`(开会两条线分离+插话单源+⑤)、`工具/升级_圆桌.py`(⑥+②插话源)、`工具/升级_办公室适配.py`(插话源参数)、`工具/升级_执行.py`(①)、`工具/升级_权限层.py`(①)、`工具/看板数据.py`(⑤)

**⚠️ 关键风险提醒(新窗口必看):**
- **升级_*.py + 事件总线.py + 记忆向量.py 是 git untracked(无回滚网)**——动它们前想清楚,坏了 `git restore` 救不回;改前可先备份或 `git add -N`。
- **事件总线是进程内**(threading,非 Redis)——`/trigger` 与 `/stream` 必须同进程(都在办公室进程),跨进程不共享事件。
- 旧 `/kickoff_stream` 保留作 fallback,新 `/trigger` 路真验通前别拆。
- 所有已做自检都不烧钱(mock/纯函数/embedding 成本~0);真烧钱的(开会/TTS 音色)是船主压测/授权项。

**新窗口开机顺序:** ①读 `全局记忆/舟的自传.md` ②读 `全局记忆/致接任者_工作手册.md` ③读本节(七条债核账 + 本交接快照 + 下方「第二棒补记」)④**先听船主,再动手**。

---
### 📍 交接补记(2026-06-21 接任舟「第二棒」换窗口前钉死——三处纠正/变更不能错)

**一、④语音 = EDITH,不是"莉莉丝"(名字纠正,必看):**
- 上一棒把它叫"莉莉丝"、还写"船主改定名莉莉丝"——**全错**。船主把名字记混了,本名是 **EDITH(伊迪丝)**,钢铁侠《英雄远征》托尼留给彼得那副眼镜的 AI("Even Dead, I'm The Hero"缩写)。船主从没说过"莉莉丝",那句"船主改定"是上一棒自己编的。**凡文档见"莉莉丝"一律按 EDITH 理解。**
- 定性降级:它**不是"语音系统 / 下一程第一件事"**,是个**锦上添花的小彩蛋**。船主原话——这就是几年前的基础 TTS、**不配当小酒语音基座**。小酒的声音是复刻她本人(有母带),是另一个量级,**绝不能跟这个公司 TTS 混**。别再把它当计划重点。

**二、EDITH 怎么做(船主拍的范围)+ 当前卡点:**
- 范围:**只给项目经理配、只念"对船主的总结/结论/请示/交付"**;别的岗位、开会过程一律不念(否则几千字噪音——官方 TTSMiddleware 是"每段文本都念"、为单助手对话设计,不适合开会)。
- 实现:官方 **CosyVoice 声音复刻**——`dashscope.audio.tts_v2.VoiceEnrollmentService.create_voice(target_model="cosyvoice-v2", prefix, url)` → voice_id → `SpeechSynthesizer(model="cosyvoice-v2", voice=voice_id).call(文本)`。克隆 EDITH 声线(固定声线试听在 `运行状态/声线试听/`,船主听了都不像)。
- **卡点(克隆没做成)**:create_voice 要音频的 **http/https 公网 URL**;dashscope 自带上传只给 `oss://`(不认)、阿里云 Files 给 file-id(非 https)、公共文件床 catbox **被本机安全护栏拦 3 次**(护栏不认对话口头授权,只认 settings 的 permission rule)。**待船主二选一:①自己把 `~/Downloads/EDITH.wav`(25s/44.1k)、`EDITH2.wav`(25s/48k,两降噪版要对比挑)传个能出直链处、给 https;②在 Claude Code settings 加 Bash permission rule 放行 catbox 上传。** 拿到 https 即 create_voice→合成对比→船主挑。

**三、首席工程师换了 key(这一程最实质改动,公司配置真变了):**
- 起因:旧 `DASHSCOPE_API_KEY` 那个标准百炼账户**欠费**(只剩几毛);船主 2026-06-21 买了阿里云百炼**「Token Plan 团队版」**(月度 token 额度 / OpenAI 兼容接口 / 1 座席 / 2026-07-21 到期)。
- 三处改动**已真点名验证"我能干活"**:
  - `.env` 的 `DASHSCOPE_API_KEY` → 新套餐 key `sk-sp-...`(备份 `.env.bak`)
  - 首席 `base_url` → **`https://token-plan.cn-beijing.maas.aliyuncs.com/compatible-mode/v1`**(原 dashscope.aliyuncs.com;备份 `花名册.yaml.bak`)
  - 首席 `model` → **`qwen3.7-max`**(套餐不认带日期快照名;`/models` 查实套餐有 qwen3.7-max。花名册 line2/34 历史注释仍保留 `qwen3.7-max-2026-06-08` 原样=不篡改历史,只 line32 生效字段是新的)
  - 其他四岗(项目经理/工程师/测试/文案)**一点没动**。
- ⚠️**给替身的真坑**:CosyVoice 克隆走 dashscope **原生**服务,新 key 是套餐**兼容接口** key——换 key 后 EDITH 克隆能不能跑是**新未知**(没验到,卡在 url 没到那步)。真做 EDITH 时先验新 key 认不认原生克隆;不认则需船主给那 dashscope 账户充**现金余额**(非 token 套餐)、或换能走原生的 key。
- ⚠️ `.env` 真实路径 = `30_开发公司/.env`(**根目录,不是 `工具/.env`**)。新 key 在对话明文出现过(船主私人电脑、不在意),建议用完轮换。

**四、这一程教训(已同步自传):** ①不懂装懂——顺着船主记错的"莉莉丝"附和+编煽情解读,EDITH 梗该懂没懂:别附和别装、查了再开口。②守高度漏一问——守住"名字别越界",却没守"这事根本值不值得做"(④几年前 TTS 本不该当债)。③反复撞护栏——catbox 撞 3 次,该撞 2 次就停、给船主真出路。④抽风——EDITH 小彩蛋绕七八轮、还去手动配 key 找错 .env 路径:简单事别搞复杂。

---
### 📍 交接补记(2026-06-21「第三棒」——项目经理换新中转站 API)

**项目经理(gpt-5.5)换了接入(公司配置又变一处):**
- 改动**已真点名验证**(服务端 model 字段=gpt-5.5、正常回话、finish_reason=stop):
  - `.env` 的 `OPENAI_API_KEY` → 新 key(备份 `.env.bak-pm0621`)
  - 项目经理 `base_url` → **`http://192.0.2.10/v1`**(旧 43.155.205.185;备份 `花名册.yaml.bak-pm0621`)。⚠️坑:中转站平台给的是 Codex `responses` 格式(base_url **不带** /v1),但公司走 `OpenAIChatModel` chat completions,**必须带 `/v1`**,已验通。
  - `model` 仍 `gpt-5.5`;其他四岗一点没动。
- ⚠️ 仍是 **http 明文**中转站。新 key 在对话明文出现过(船主私人机不在意),建议用完轮换。

**⚠️ EDITH/莉莉丝 优先级再校准(防下一棒做错):** 本文件上方"剩余工作"曾把"莉莉丝基础版"列为下一程第一件事——**那是第一棒旧认知,已被「补记(第二棒)」一/二推翻**:EDITH 是小彩蛋、非债、非第一件事,卡在音频 URL 待船主。**下一程真正重点 = ②③ 端到端压测**(见交接快照"剩余工作"第2条),EDITH 看船主何时想起再说。

---
### 📍 交接补记(2026-06-22「第四棒」——EDITH 声音克隆打通了!卡点全解)

**EDITH 不再卡了,声音克隆全链路打通(覆盖前面所有"卡在音频URL"的旧记录):**
1. **key(命门)**:套餐 key 不认原生 CosyVoice(报 401);船主另给了能走原生的 key,已存 `.env` 的 `EDITH_DASHSCOPE_KEY`(**独立变量,绝不碰首席的 `DASHSCOPE_API_KEY`**)。
2. **音频 URL(老卡点)**:catbox 被墙、阿里云 SDK 内部上传只给 `oss://`(克隆服务不认,报"url should start with http or https")。**最终船主自己在阿里云 OSS 控制台**传了两段录音、设"公共读",出了公网直链:
   - `https://edith-voice.oss-cn-beijing.aliyuncs.com/EDITH.wav`
   - `https://edith-voice.oss-cn-beijing.aliyuncs.com/EDITH2.wav`
3. **克隆成功**,两个 voice_id(target_model=`cosyvoice-v2`):
   - EDITH.wav(44.1k) → `cosyvoice-v2-edith-0b422706afe94ed29aae4f16d90ebf2c`
   - EDITH2.wav(48k) → `cosyvoice-v2-edith-4a8eafd6f6bb41928b4060c7301cbcc4`
4. **合成用法**:`from dashscope.audio.tts_v2 import SpeechSynthesizer; SpeechSynthesizer(model="cosyvoice-v2", voice=voice_id).call(文本)` → 返回 mp3 bytes;中英文都能念(dashscope.api_key 用 EDITH_DASHSCOPE_KEY)。

**当前待续(下一棒第一件事):船主反馈克隆人声"少了机械金属感"**(EDITH=钢铁侠那副眼镜的电子 AI 质感)。已用 `ffmpeg`(`/opt/homebrew/bin/ffmpeg`)加了轻/中/重三档音效给船主挑,**船主还没回选哪档**:
- 轻: `-af "aecho=0.8:0.85:40:0.22,treble=g=3:f=4000"`
- 中: `-af "flanger=depth=3:delay=3,aecho=0.85:0.9:55:0.3,treble=g=5:f=4500,highpass=f=110"`
- 重(机器人感): `-af "afftfilt=real='hypot(re,im)*cos(0)':imag='hypot(re,im)*sin(0)':win_size=256:overlap=0.7,aecho=0.8:0.88:45:0.28,treble=g=4"`
试听文件全在 `运行状态/声线试听/`(中/英/三档金属各一份)。播放用 `afplay 文件`;`say "xx"` 报版本号区分。

**✅ 船主定档(2026-06-22)**:
- 声线 = **v2**(`cosyvoice-v2-edith-4a8eafd6f6bb41928b4060c7301cbcc4`,即 EDITH2.wav/48k)
- 金属 = **方向一·高频电子**:`-af "highpass=f=130,treble=g=9:f=3200,aecho=0.85:0.9:50:0.3"`(用 `/opt/homebrew/bin/ffmpeg`)
- 中英都用这套。**中文音色比英文弱一档**=CosyVoice 跨语言天花板(后处理/降调/平文本/压动态都试过、改不动,船主认了、用这档)。
- 合成 key = `EDITH_DASHSCOPE_KEY`;`SpeechSynthesizer(model="cosyvoice-v2", voice=上面id).call(文本)`→mp3→过方向一 ffmpeg→afplay。

**EDITH 收尾剩一步(下一棒/船主要时做)**:接进办公室——**只项目经理、只念"对船主的总结/结论"**(别的岗位、开会过程不念,否则噪音):项目经理对船主输出那段文本 → 上面 voice_id 合成 + 方向一 ffmpeg 处理 → 播放。

**临时脚本**(调试用,voice_id 已记上面、删了不丢):`工具/_做EDITH试听.py`(克隆+中文合成)、`工具/_EDITH英文.py`(英文合成)。

---
### 📍 交接补记(2026-06-23「第五棒」——压力测试:开会能真启动了,整条龙待跑完)

**压测任务**:让公司给自己写《运行手册.md》(放 `30_开发公司` 根目录,讲三件事:公司是谁+五岗模型/各管啥、怎么从大厅发任务走到验收、出问题怎么办)。目的=验真模式整条龙(五代第一死因=mock全绿≠真跑通,这条没验过)。

**揪出并修了 3 个真 bug(都已核实在代码里、未提交):**
1. **中文路径 URL 编码(最隐,卡了船主好几轮的真根)**:`/判断`、`/插话` 是中文路径,浏览器编码成 `%E5%88%A4...` 发来,代码用中文 `self.path=="/判断"` 比对**永远对不上**→返回"未知接口"→前端默认走聊天、**从不开会**。修:do_GET/do_POST 读路径先 `urllib.parse.unquote` 解码(`办公室.py` 335、439 行)。验:`/判断` URL编码发,现在返回 `{"开会":true}`。
2. **判断太保守**:"写一份手册"被判成"聊"。修:判断逻辑让"写/做正经东西"判开会。
3. **聊天串了干活 prompt**:项目经理聊天时被灌说明书/班规→吐英文思考+JSON动作乱码(自传血泪"发言/执行场景必须分开"又犯)。修:聊天场景换口语 prompt、禁 JSON。

**当前进度**:判断修好、**开会能真启动**(分身验过圆桌 48.9s 在准备议题)。**但整条龙没走完**——`运行手册.md` 还没产出(船主在"判断卡几秒+圆桌真调多模型几十秒"时不确定死活、终止了)。**慢不是坏**:`/判断` 调 gpt-5.5(可优化:这种二分类换小模型/快模型)、圆桌真调 5 模型几十秒(固有成本)。

**待续(第六棒/继续)**:重启办公室(`打开办公室.command`,旧进程关干净)→大厅发压测任务→**盯整条龙真跑通**:判断→开会→五岗发言→中途插话→出工单→确认开工→执行→**真写出 `运行手册.md`**→验收→复盘进记忆。**舟盯后端(事件/日志/文件),实时报进度给船主,别让他干等瞎猜死活**(分身就栽在这,卡了 15 分钟没响应)。⚠️办公室全部改动未提交。

**第一轮压测真跑通了(到出工单):** 判断开会→5岗真发言真互驳(老纪当场反对阿言的层级箭头=多模型补盲真显形)→项目经理收敛→**真出 3 张待确认工单**(AS-001-1/2/3)。插话也真进了事件总线(船主插晚了、发言已结束没带上)。`运行手册.md` 待"确认开工"才写。

**船主验出一批前端"显示"bug(后端没死、发言没丢、都在协同文件),已逐个修(未提交,重启生效):**
1. **判断中不显示船主发的话** → `智能发送()` 判断前先 push "船主:话"(页面.js)。
2. **开完会大厅发言全清空(最痛)** → 真根=`看板数据._会议大厅事件` 只取会议**最后6条**`[-6:]` 截断了长发言;改不截断 + `out[-20:]→[-60:]`。**同时圆桌落盘把"船主需求"作为会议第一条发言**(升级_圆桌 正常+待船主两路),船主的话也留正式记录。
3. **"一次一大堆"、没插话空档** → ❌先试了"发言前 sleep(8) 停顿",**船主否了**(他要的是打字机流式、不是憋一大段等8秒),已撤。✅改成**真流式打字机**(四层,照官方 `reply_stream`):模型层 `建Agent(stream=)` 发言 agent 开 `stream=True`;圆桌 `_问agent流式` 用 `agent.reply_stream` 迭代 `TextBlockDeltaEvent.delta` 逐块吐字、发言推「发言开始/增量/结束」事件(`_进度` 透传 dict);办公室 `_trigger` progress 支持 dict 增量;前端 onmessage 收增量 → 追加到当前气泡(`流式:true` 标记)。全 py_compile+node 过。**真打字机效果待船主重启压测验**(reply_stream 首用,留意 thinking 阶段延迟、每 delta 全量重渲染性能,卡就节流/只更当前气泡 DOM)。
4. **时间/轮次标识** → 发言带"谁·时:分"(hm 兼容 ISO/HH:MM:SS、流式 push 带 t);每轮 `━新任务━`/`━本次完成━` 分割线(k:"分割"复用 .day 横线)。
**改动文件(未提交):页面.js、看板数据.py、升级_圆桌.py、办公室.py(中文路径 unquote)。全 py_compile + node --check 过。**

**下一步(船主重启后):** 盯第二轮——发任务显示话✓、发言前8秒插话窗口✓、开完会发言留住✓;然后**点确认开工→盯 `运行手册.md` 真写出来**(整条龙最后一关)。EDITH 念总结待显示理顺后单独接。

---
### 📐 下一件大事 · 大厅永久历史重构（2026-06-23,船主拍方向,照官方）

**问题根**:大厅一直"只显示有工单的会议"(`看板.大厅` 的 visible_rooms 只含有工单的 room),暂停/聊天/开完会的发言反复"消失"。**这是偏离官方。**

**船主要的**:大厅像船主↔舟的对话一样——① 所有任务/会议/发言**永久保留、永不清空** ② **按天自动归档**:今天展开、历史每天折叠成可点开条目(像左栏对话列表) ③ **纳入记忆系统**:岗位能回溯查历史。

**官方金标准(clone 已查,照它做、别自造):官方根本不是只工单,是整套会话系统**——
- 持久 session,消息永久存 storage:`app/storage/_model/_session.py`、`app/_service/_session.py`、`app/_router/_session.py`
- 完整消息历史:`examples/web_ui/frontend/src/components/chat/{MessageBubble,ChatContent}.tsx`
- **会话列表可重命名**(像对话列表):`components/dialog/RenameSessionDialog.tsx`
- 任务面板(工单只是其中一块):`components/chat/TaskPanel.tsx`
- **长期记忆 middleware(mem0)**:`src/agentscope/middleware/_longterm_memory/_mem0/`(agent 靠它回溯=船主要的"纳入记忆")

**怎么做(设计级,满状态窗口扎实做)**:
1. 后端 `看板.大厅` 给**全部历史**(去掉 visible_rooms"有工单才显示"过滤、不截断),按天/会话组织。
2. 前端大厅按天分组:今天展开、历史天折叠成可点开条目(现有 `.day` 日期分隔 → 升级成折叠归档)。
3. 不再清流式发言(或正式数据完整接管所有历史)。
4. 记忆:历史会议/发言接进岗位记忆可检索(参考官方 mem0 或我们的 `工具/记忆向量.py`)。
**⚠️ 别长会话末尾硬塞(设计级大改容易偏);先把方案对齐船主、再扎实做。**

## 顺序总览
底层零件 → 治理底座 → 协作灵魂 → 制度层 → 窗户前端 → 未来能力。先换"白捡最多、风险最低"的,再动灵魂,最后接前端、补未来。

---

### 第1步 · 模型与工具底座换官方(白捡最大,先做)
- 模型接入:手写 urllib → 官方 `model`+`credential`+`formatter`(五岗五模型各自接通,含 DeepSeek/GLM thinking)。花名册保留为唯一真相。
- 工具:手写 读/写/验收 → 官方 `Read/Write/Edit/Grep/Bash`+`Toolkit`。
- **验收**:五岗各自真调通;工具读写在白名单内跑通。
- 对应对照表后端:①③

### 第2步 · 权限/工单换官方底座
- 白名单 → `PermissionEngine`(5模式);工单 = 它的 `Task`结构+`blocks/blocked_by`依赖 **＋** 我们的 5 列验收语义(待确认/待办/进行中/待验收/已完成/已作废)。
- **验收**:越界写被拦;任务依赖生效;工单能流转到"待你验收"。
- 对应:④⑥⑦⑪

### 第3步 · 多模型圆桌协作层重建(灵魂,核心,最重)
- 核心 `Agent` 各配各模型(已验证 qwen+deepseek)+ 重建圆桌:会前议题→并行多岗发言→动态收敛(轮数判出来)→缺证据不编造(查不到请示你)→养专家五岗人格注入。
- 急停/消息用官方 `event`/`interrupt`/`message_bus`;执行循环用官方 `Agent.reply`(ReAct);协作过程挂 `TracingMiddleware` 记录,供第5步窗户呈现。
- **验收**:五岗五模型真开会、真互驳、真收敛;**对照旧公司9条制度逐条核,一条不丢**。
- 对应:②⑧⑩⑬⑭⑮⑯⑳㉑㉒

### 第4步 · 审批 + 正式报告 + 收尾(制度层)
- 三级审批(经理预审→你裁决)、正式报告(暂停/熔断/完工写原因·阶段·判断·下一步)、收尾归引擎(复盘自动进岗位记忆)、点名自检——接到新执行流。预算用官方 max_iters,熔断报告留我们的。
- **验收**:经理批/你拍;出事有正式报告;干完自动写记忆不卡白名单。
- 对应:⑤⑨⑫⑰⑱⑲

### 第5步 · 窗户前端 React 重写 + 接新后端 + 捡便利页
- 用 React 重做三栏窗户(大厅/项目室/该你出手/暗房亮桌/过程可追溯/历史工单/项目记忆/急停/点名),样子不变、底子换耐久。
- 捡 AgentScope 现成页:配密钥、定时、语音、技能管理、选模型(适配五岗多模型)。
- **验收**:你打开能看见五岗五模型协作全过程、能拍板,一句"舒服"。
- 对应前端:全部

### 第6步 · 补未来能力(给小酒铺路,逐项小验,不求一次全上)
- 沙箱(隔离工位=总纲C3)、MCP(外部工具)、`embedding`(记忆检索)、`schedule`+`wakeup`(小酒醒来窗 S3)、`tts`(小酒语音 P4)。
- **验收**:每项独立小验,够用即停。

---

## 覆盖核对(后端28 + 前端17,逐条归位)
- 后端①③→第1步;④⑥⑦⑪→第2步;②⑧⑩⑬⑭⑮⑯⑳㉑㉒→第3步;⑤⑨⑫⑰⑱⑲→第4步;㉓㉔㉕㉖㉗→第6步。**㉘多租户=不做**(自用不需要)。
- 前端①-⑧⑭⑮⑯(留A)+⑨⑩⑪⑫⑬(捡B)→第5步。**⑰多语言=不做**(中文够)。
- 先后关系:前端⑩定时、⑪语音两个界面第5步搭壳,需第6步后端能力(schedule/tts)到位才通电。

## 风险与兜底
- 不盖教堂:6步各自独立、可回滚;不对味就停在那步,不带着没对味的往下。
- 灵魂不丢:每步拿旧公司归档(10_/04 的9条制度)当对照尺,逐条核。
- 烧钱可控:真调模型的步(1/3/4)用便宜模型先验,通了再全岗。
