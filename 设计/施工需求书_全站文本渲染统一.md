# 施工需求书：全站文本渲染统一（2026-07-04 实例主人令："在哪说话都别给我那种东西"）

现状：大厅气泡已有轻量渲染器（HallView.tsx 里的 renderLightText/renderLightLine/renderInline/MsgTable：
**加粗**、`代码`、- 列表、###→小节题、---→细分隔、|表格|→迷你表格）。其他显示模型文字的地方仍在裸奔 markdown。

## 施工内容
1. 抽取：把 renderLightText / renderLightLine / renderInline / MsgTable / 相关辅助函数从 HallView.tsx
   移到新文件 前端/src/components/文本渲染.tsx，导出 renderLightText（与 MsgTable 内用）。HallView 改为 import。样式类名不变。
2. 铺满以下位置（把裸 {text} 换成 renderLightText(text)，保持各处现有字号/颜色样式外壳不动）：
   - Worksite.tsx：单元展开里的「发言」全文与摘要行不必（摘要是单行截断，保持纯文本）；「动作」结果全文保持等宽原样（工具输出不是话）。→ 只改「发言」全文区。
   - HoloChrome.tsx 星球弹卡：「脑内」保持原样等宽（思维流是意识流不是发言）；不改。→ 本文件不动，报告里说明即可。
   - HallView.tsx 会议展开/回看时间线的每条发言正文。
   - MemoryView.tsx：近况/原则 prose 区、待审/叮嘱/记事条目文本。
   - Pages.tsx：DecisionsView 的 line-detail（请示内容）、ScenesView 事件正文（若显示模型长文）。
   - Room.tsx：项目室里显示模型发言/报告正文的位置。
3. 防御：renderLightText 输入可能含超长行/超多行——保持现有实现的性能特征（纯同步、无重库）。零新依赖。

## 红线
零新依赖；不改数据层/hallStore/后端；样式如需补充只追加 holographic.css 尾部；不动 HaloCanvas。

## 自测与交付
- npm run build 过；
- 用含 ###/|表格|/---/加粗 的假文本在上述每个位置人工核验一处截图级描述（写进报告）；
- 交付 设计/施工报告_全站文本渲染.md：改动清单+各位置前后对照说明。
