# 施工报告 · Codex

## 本次施工

- 按 `施工需求书_给Codex.md` 与三份母版 HTML，把 `前端/` 视觉层切到「深空全息」。
- 新增 `HaloCanvas`：移植三态粒子光环参数，支持 `idle / working / verdict`，并按 `prefers-reduced-motion` 降级。
- 新增 `HoloChrome`：承载无框顶栏、四角读数、红点提醒、待命引线、工作中三人悬浮位、待拍板中央打板层。
- `App` 接入三态判断：待裁决/待验收优先进入 `verdict`；工作中进入 `working`；其余为 `idle`。
- `HallView` 保留原有发送、附件、SSE、匀速打字机、工作现场逻辑，只替换为系统级文案和深空指令行。
- 旧 `暗房亮桌.css` 保留，但入口已改为 `styles/holographic.css`，不再引用。
- `Room` 没改数据逻辑，通过新样式做暗色系适配。

## 对照母版自检

- `光环_动态预览_母版.html`：近黑底、光谱粒子环、斜纹肌理、四角读数、MUNAEA 描边水印、无框导航、待命引线和底线式指令行已还原。
- `开机_动态预览_母版.html`：工作态环收紧加速，真实 `活人单元` 渲染为三组悬浮工作位，保留实时插话入口。
- `打板_动态预览_母版.html`：待验收/待裁决进入中央悬浮打板层，批准动作带 60ms 白闪。
- 红点提醒接 `待办.活公司待验收 + 待办.待裁决`，不显示数字徽章。
- 示例母版内容没有写进组件；问候语“主人”按需求暂硬编码并已标 TODO。

## 校验

- `npx tsc --noEmit` 通过。
- `npm run build` 通过。

## 2026-08-12 · 大厅语义查岗与临时经理租约

- 大厅查岗改成语义分流，不再靠“谁在 / 谁空 / 除了老钟谁在”这类穷举词表硬猜；`who_is_free` 重新回到只发言场景的真实权限里，专门查在岗/空闲/可接活。
- 正式任务点到项目经理时，先尝试原经理；若原经理掉线，则当前会话挂临时统筹租约，由按现任次序排出的代理岗位接手，原经理回来后自动退租恢复正常。
- 这条临时代理只改会话内承接，不改职级、不改晋升链，也不会同时留下两个经理。
- 联动文件：`工具/经理代理.py`、`工具/大厅.py`、`工具/交付门.py`、`工具/活_本人.py`、`工具/常识门.py`、`工具/平台_工具集.py`、`工具/部门/__init__.py`、`测试/回归.py`。
- 回归 `102/102` 全绿。

## 2026-07-02 返工 · 班子页轨道花名册

- 按船主「轨道花名册」图纸重画班子页：桌面态废掉表格，五个岗位固定在 Halo 光环轨道上，节点向外放射引线并悬挂名牌；船主作为环下方地面站，不进轨道。
- `HaloCanvas` 导出 `projectHaloPoint(u, frame)` 纯函数，并每帧广播当前投影几何；班子页 DOM 节点使用同一套 `rx/ry/tiltK/roll/breath` 坐标，跟随光环摆动。
- 五人固定角度：老钟 `3.5`、老梁 `2.45`、老纪 `1.55`、阿强 `0.55`、阿言 `5.5`；工作中成员节点变琥珀并呼吸。
- 名牌改为无框全息引线：名字、岗位+模型、hover 能力三行；左半边右对齐，右半边左对齐，能力用 `·` 分隔。
- `CREW · 班子` 与 `点名` 移到左右上读数位；页面中央只留光环、节点、引线、名牌和船主地面站。
- 班子页光环透明度压到 `.8`；`<1180px` 保留原表格式 fallback。
- 顺手统一相关字号：名字 16px/500，mono 元数据 10.5px/2px 字距，正文 13px，kicker 11px/4px 字距，行高 1.6。
- 未触碰 `工具/`，未新增依赖。

校验：

- `npx tsc --noEmit` 通过。
- `npm run build` 通过。
- `npm run build` 通过。

## 留给真机验收

- 后端真实 `/board`、`/hall`、`/stream` 数据下的三态切换观感。
- 真实活公司待验收卡的批准/打回链路。
- 真实长会议/多活人单元时，三人悬浮位和大厅工作现场的阅读密度。

## Commit 计划

当前环境阻止写入 `.git/index.lock`，`git commit` 返回 `Operation not permitted`，所以未能实际落 commit。建议提交为：

1. `feat(frontend): 接入深空全息外壳`
2. `style(frontend): 重塑大厅与项目室全息视觉`
3. `docs(design): 补充 Codex 施工报告`

## 2026-07-02 返工 · 大厅收纳

- 按 `C_窗户设计定稿_深空全息.md` 收纳军规重切大厅：待命态只保留顶导航、HaloCanvas、两条引线标注、问候语、底部指令行、四角状态读数。
- 删除 React 入口里的左右栏渲染，并移除旧 `Rail` / `Action` 组件；大厅标题、历史 feed、空状态文案、进行中计数、右栏四区块全部不再出现在大厅 DOM。
- 新增 `班子` 页：承载班子列表和点名，使用无框引线式排版。
- 新增 `场次` 页：按天列出大厅历史对话条数，点日期进入当天对话。
- 新增 `待你拍板` 页：承载旧待确认、裁决、回炉、活公司验收、旧待验收数据；动作统一为文字+琥珀底线。
- 大厅待拍板数据改为右侧引线标注：有待处理内容才显示，零态不渲染该标注；导航红点保留。
- 停止动作只在公司工作中出现在底部指令行右侧。
- 未触碰 `工具/`，未新增依赖。

校验：

- `npx tsc --noEmit` 通过。
- `npm run build` 通过。

## 2026-07-02 修补 · 光环锚点与班子健康灯

- 大厅待命态两条引线不再使用固定 SVG 坐标：`核心 · 在场` 锚定 Halo 角度 `u=2.9`，`五人待命 / 待你拍板` 锚定 `u=5.85`，每帧读取 `HaloCanvas` 广播的几何并重算线端、端点圆点和文字块位置。
- `HaloCanvas` 的逐帧 frame 监听封装为前端共享 hook；大厅与班子页现在使用同一套 `projectHaloPoint` 投影机制，窗口尺寸变化和呼吸摆动时端点圆点会贴住环带。
- 前端接入 `GET /健康`：进入页面立即拉取，之后每 45 秒轮询一次；不改后端 `工具/`。
- 班子页节点与名牌前状态点按优先级显示：工作中琥珀呼吸 > 健康失败红色呼吸 > 健康成功白 60% > 未返回灰 25%。
- 健康失败的名牌下方追加红色 mono 说明行，格式为 `原因 · HH:MM`；右下角 `CREW 5` 在存在失败岗位时显示 `CREW 5 · N DOWN` 并转红。
- 名牌里的岗位/模型行改为单行省略，避免阿言 `文案工程师（兼职） · DeepSeek...` 折行。

校验：

- `npx tsc --noEmit` 通过。

## 2026-07-02 真机修补 · HallView 执行按钮与常驻字幕栈

- Bug1：指令行右侧补真按钮 `执行`，工作中切成 `插话`；按钮为文字动作样式，hover 出琥珀底线。
- 键盘改为 Enter 直接发送，Shift+Enter 换行，保留 Command/Ctrl+Enter 发送；中文输入法组字期间不误触发发送。
- 指令提示删掉不存在的“空格按住说话”，只保留待命 `↵ 执行` / 工作中 `↵ 插话`；`停止` 仍只在工作中出现。
- Bug2：大厅新增无框字幕栈，位于指令行上方，max 720px 居中，底部对齐向上生长，最多显示最近 12 条。
- 待命时字幕栈读取 `/hall` 当天 `k='话'` 事件；发送/工作中优先读取 `hallStore.临时`，让现有流式打字机继续接管同一栈，run 结束后临时内容保留到下次发送或刷新。
- 字幕样式按船主/公司两侧排版：船主右对齐、琥珀 60% 时间头、正文白 85%；公司左对齐、白 35% 时间头、正文白 70%；正文 14.5px / 1.7。
- 流式发言保留块状 caret；字幕区无底色，不遮光环。
- 问候语 `晚上好，主人` 只在字幕栈为空时显示。
- 未触碰 `工具/`，未新增依赖。

校验：

- `npx tsc --noEmit` 通过。
- `npm run build` 通过。

## 2026-07-02 批量修补 · 字幕栈、滚动、轻渲染、读数、打板白闪

- HallView 的字幕栈改为「当天持久话语 + 临时流式话语」永远合并显示；临时条目会与持久尾部 20 条按 `who + 去空白 text` 去重，最终只取最近 14 条，发送新消息不再清空可见历史。
- 字幕栈滚动改为可解锁：上滚时停止自动贴底，滚回距底 60px 内恢复贴底；容器保留 `overflow-y:auto`，高度按视口扣掉问候语/指令行空间约束。
- 字幕正文增加安全轻渲染：定稿后解析 `**粗体**`、`` `行内代码` ``、行首 `- `/`• `、单双换行；流式中仍按原文显示，避免半截 Markdown 闪烁。
- HoloChrome 右下读数从假 `MEM +N` 改成 `SCENES {项目数} · CREW {人数}[ · N DOWN]`；「记忆库」「设置」标为未接入 disabled 态。
- 待你拍板页点击「批准 · 打板」并且后端验收成功后，触发全屏白闪：先 60ms 保持 `opacity .9`，再用 500ms 淡出。
- 未触碰 `工具/`，未新增依赖。

校验：

- `npx tsc --noEmit` 通过。
- `npm run build` 通过。

## 2026-07-02 可读性升级 · 对话玻璃与光环让位

- 大厅字幕栈每条消息正文改为 `.glass-msg` 磨砂玻璃块：无描边、20px 大圆角、13px/18px 内边距、仅保留顶缘内高光；头行与正文同在玻璃内部，流式打字也在同一玻璃块中进行。
- 船主消息玻璃使用微暖底色 `rgba(30,24,15,.4)`；公司消息使用 `rgba(16,19,26,.45)`。
- `HaloCanvas` 增加 `recede`：大厅存在对话或工作中时，光环以约 `0.06/帧` 缓动退后到 0.8 缩放、中心 `H*0.30`、透明度 0.65；退出对话后缓动回原位。
- 光环每帧广播的是退让后的同一份投影几何，所以大厅引线端点继续跟随真实环带。
- 字幕栈滚动条改为 4px，透明轨道，thumb `rgba(255,255,255,.14)`，hover 为 `.25`。
- 设计军规修订记录：『玻璃容器在可读性场合（对话消息）获得豁免：必须无描边+大圆角+磨砂+仅顶缘高光』。
- 未触碰 `工具/`，未新增依赖。

校验：

- `npx tsc --noEmit` 通过，0 个类型错误。
- `npm run build` 通过。
