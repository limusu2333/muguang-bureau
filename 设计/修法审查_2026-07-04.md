# 七修法对抗审查 · 2026-07-04

> **历史审查记录，不是现行架构说明。** 文件中的旧入口、旧搜索方式和行号只用于追溯；当前事实以根 `README.md`、现役代码和最新实测为准。

守门人：只读代码、只判"别改坏"。逐条对着真代码核过 办公室.py / 活_本人.py / 会议室.py / 执行室.py / 常识门.py / 大厅记录.py / 平台_工具集.py。

七条落点速览：
- G-1：活_本人.py(新增_记一次唤醒~740、删809-815) + 会议室.py(146-154、225-233 可找人=False→只发言=True)
- G-2：活_本人.py:108-112(指路牌) + 办公室.py:673-680(点名闸门)
- G-3：活_本人.py:382区(构造聊天消息) + 348(聊天系统提示)
- G-4：常识门.py:93前新增近音函数 + 103-109(_提及检测)
- G-5：办公室.py:788(已转经理统筹=True)
- G-7：执行室.py:95 + 活_本人.py:740/744 + 办公室.py:841-845/867
- G-8：大厅记录.py:32 + 办公室.py:833/846-848

---

## ① conflicts（碰同一段 / 互相打架）

### C1【最硬 · 必须合并应用】G-7 与 G-8 贴在 办公室.py 845~848
- G-7 改 841-845：`task = loop.create_task(_main())` → `loop.create_task(_带总墙钟(_main(),1200))`，且 845 行 `报告 = loop.run_until_complete(task)` 保留但语义变（现在会抛 TimeoutError）。
- G-8 改 846-848：`if 报告 and "[沉默]" in 报告` 那段。
- 两条编辑区**相邻且共享 845 这一锚点**。若各自用字符串匹配 Edit，先应用谁、后一条的 old_string 可能已漂移。**必须知道彼此存在、按同一份最新代码改**，否则第二条 Edit 失配或误改。判为需协调，不判为逻辑冲突。

### C2【语义耦合 · 需一起想】G-1 的"删 809-815 计数块" 改变了 G-7 依赖的现场
- G-1 改动一要求**删掉 809-815 整段**（原按 ToolResultEnd 记 `_会话动作数`），改到入口 `_记一次唤醒` 计数。
- G-7 完全不碰这段，但 G-7 的"卡死墙钟"和 G-1 的"预算熔断"都在 活_本人.py 的同一 while 循环里加/删代码。两条都动 730~815 这段热点。**不冲突但必须以对方改完的版本为基线**：G-1 删 809-815、G-7 在 740/744 加超时，行号会互相位移。

### C3【设计打架 · 重点】G-1 的"按唤醒计数" 与"会议裁决/发言也走唤醒本人"叠加，可能误伤 G-5/正常会议
- `_有效会话id = 会话id or _会话id_var.get()`（742 行已确认）——会议里主席裁决(会议室.py:80 `唤醒本人(主席,…,只发言=True)`)和每个参会发言(225)**都会拿到同一个 sid**（contextvar 透传），于是 G-1 的 `_记一次唤醒` 会给会议里每一次裁决+发言各记一笔。
- G-1 自己也承认"30 轮会议≈60 笔"。但 `_会议失控上限=30`（会议室.py:15）——一场满轮会议就烧掉 ~60 笔，占 200 硬上限的 30%。**两三场合理会议就撞熔断**。这与"200 对真实工作量够不够"强相关，属设计问题不是 bug，但 G-1 把"会议轮次"也计入总量池，等于用同一根绳同时勒"老钟反复开会"和"单场会议内部轮次"——后者是会议室.py:15 已有的 30 上限在管的，现在被 G-1 二次收紧。需船主知情：**200 这个数在改动一之后含义变了**（从"调工具次数"变成"唤醒次数=裁决+发言+派活+每步ReAct动作全混一池"）。

### C4【无真冲突，澄清】G-2 RC-B 与 G-1 改动二都碰"唤醒/闸门"但不同段
- G-2 RC-B 改 办公室.py:673-680（被点名分流），G-1 改动二改 会议室.py:146-154/225-233。两处物理隔离，无冲突。
- 但注意 G-2 RC-B 删掉 676 的 `_是任务话` 判断后，**被点名一律进办事底盘**；而 G-1 的入口唤醒计数会给这些"@闲聊也上办事底盘"的场景也记账——两条叠加会让"@某人说句闲话"也吃掉唤醒预算。量小，可接受，但需知道 G-2 RC-B 放大了 G-1 的计数入口。

### C5 G-3 两处、G-1 会议两处、G-4 常识门 均在各自独立文件段，互不重叠。G-5(788) 与 G-8(833/846) 在 办公室.py 但行距足够、锚点不同，无字符串失配风险。

---

## ② regression_risk（每条可能弄坏什么）

### G-1 回归面（中高）
1. **会议被 200 过早掐死**：见 C3。真实"一场大会 + 老钟复议一次"就可能逼近上限，出现"唤醒数已达硬上限"把正经会议截断。原来纯文本会议不计数=可以开很久，改后会议是计数大户。这是行为反转，可能误伤合理长会。
2. **改动二让参会人失去查证能力**：`可找人=False` 走的是"协作/判断模式"（本人工具集，仍有 Read/Grep/Glob/web_search/search_hall_history）；`只发言=True` 走 `只发言工具集()`——**只有 search_hall_history / recent_hall_chats / escalate**，**没有 Read/Grep/Glob/web_search**。而 `_发言来意`(会议室.py:111/120) 明写"需要证据就先查资料，别凭空编"。改后参会人被要求查证却没了查文件/查网页的手，只剩查大厅历史。**这不是"只去掉 find_colleague"，是砍掉了会议发言人的资料室**。若会议要看代码/查外网才能发言，改后只能空口说——直接踩红线"别造假：查不到就说没查到"的反面（逼人凭印象）。**判为真回归，比 G-1 自评的"属预期"严重。**
3. 删 809-815 若删不干净（比如漏删 `_会话动作数[_有效会话id]=_n` 但留了 if）会双计或计数错乱。

### G-2 回归面（低-中）
- RC-A 只加提示文字，风险仅"提示变长"，协作模式无长度截断，安全。
- RC-B：**@某人闲聊也走办事底盘**——办事底盘系统提示重、模型可能更"想干活"，理论上更容易 escalate/写文件。但协作模式白名单无 Write（688-694 已确认无 Write/Edit），真红线在干活模式白名单那层，误写文件不成立。真实回归是"略慢略贵"+"@闲聊也吃唤醒计数(叠加G-1)"。可接受。

### G-3 回归面（低）
- 只对"最后一条船主话"加锚，判据 `_最新船主idx`。若某轮 records 尾部没有船主话（纯冷场兜底），`_最新船主idx=-1`，走原逻辑，安全（作者已覆盖）。
- 风险点：办公室.py:631 已经在办事底盘那侧做了"## 实例主人现在说"的锚。G-3 只改**聊天底盘**的 构造聊天消息/聊天系统提示，不碰办事底盘（631 那套走 唤醒本人 的 来意 拼接，不经 构造聊天消息）。两套锚不会打架，但**表述要一致**，否则同一个人在聊天/办事两条路看到两种"当前锚"措辞。低风险。

### G-4 回归面（中，误触是主要风险）
- 近音表若配宽会把常用词误判成点名→乱唤醒。作者用"仅名字/别名(2字专有)+等长+只差1字+该字属预置近音集"四重收口。**但预置近音集里有隐患**：`"言": set("炎盐严延颜岩沿研烟")` + 名字含"言"（阿言），近音命中挂在 `专有={名}|别名`——"阿言"整体2字，近音要求"阿X"里 X∈言的近音集且首字"阿"精确。像"阿炎/阿颜/阿延"会命中。真实文本里"阿颜"等不常见，风险可控；但"阿研/阿岩"这类若出现在无关语境会误唤阿言。属可接受的小误触面，但**近音集是纯静态手工表，改花名册名字必须同步改这里**，否则近音容错对新名字失效或对旧字误触——维护负担落在人身上，容易腐化。
- 回评唤醒(办公室.py:819)那道正则更严，近音只是先让 `_名` 易被识别，最终受正则收口，不会刷屏——此点作者说法与代码一致（819 的 `_re.search(rf"@{_名}|{_名}\s*[，,：:]|…")` 仍要求真呼唤结构）。

### G-5 回归面（低）
- 置 `已转经理统筹=True` 只在 `办岗=="项目经理"` 且走到 788。核对 788 上下文：788 在 `if not 回 or "[沉默]"…continue`(785) 之后，即**只有经理真说了话/办了活**才置 True。若经理直通办事抛异常(747 continue / 781 continue)则走不到 788，不置 True，同事转经理兜底仍生效——作者说法与代码一致。
- 唯一要盯：788 是 `已说岗位.add(办岗)`，经理**闲聊直通**(非"有活儿要接"路径)也会走到 788 吗？会——若经理是被常识门以"日常聊天"叫醒、聊天底盘回了非空话，也到 788。此时置 `已转经理统筹=True` 会**误挡后面真有人[转办事]转经理**。这是 missed_case，见④M2。

### G-7 回归面（中）
1. `os.killpg`/`start_new_session` 是 POSIX-only。本机 darwin，OK；但代码里若有 Windows 兜底期望会破。当前环境 macOS，安全。
2. 变量改名 r.stdout/r.stderr/r.returncode→r_stdout/r_stderr/returncode，**漏改一处就 NameError**。96-101 有三处引用（`出=(r.stdout…)`、`r.returncode`两处、`标=…r.returncode`）。必须全改。
3. `except asyncio.TimeoutError` 分支在 办公室.py except 链里**必须放在 `except CancelledError`(864) 之后、`except Exception`(867) 之前**——否则被 Exception 吞掉或顺序错。TimeoutError 是 Exception 子类，放 867 之后永不命中。
4. 墙钟常量：活_本人 `_总超时秒=900`、办公室 `1200`、执行室 `180`。若真有 15 分钟以上的合理长活（大编译/大测试）会被 900 误杀。需按真实最长活校准，别设太小。
5. 顺手修 line 748 `{人名}`→`{_人}`：确认 748 现状是 `f"{人名}的模型流…"`，`人名` 参数在派活/会议路径确实是 None（594 签名 `人名: str|None=None`，assign_task/会议 调用没传 人名），会打印 "None的模型流…"。**这是真 bug，该修**。

### G-8 回归面（低）
- 四个哨兵 `[转办事]/[沉默]/（没说话）/(没说话)` 都是机器控制字，真人发言不会等于它们，误吞面几乎为零。
- 唯一盯点：G-8 在 大厅记录.记一句 落盘口加哨兵过滤=**平台级全局拦截**。确认没有别处依赖"把[转办事]写进大厅"做状态机——搜过，记一句 的调用方都是真发言落盘，无人靠哨兵入库。安全。

---

## ③ violates_truth（写死意图 / 破坏留痕 / 假发生）

- **G-1**：改动一"按唤醒记账"符合"不许假发生"（纯文本会议原来烧真钱却不入账=假不发生）。改动二把参会人降为只发言，**去掉的是会议里的资料室**（见 G-1回归2）——这有轻微"逼人凭印象"的味道，与红线"查不到就说没查到"擦边，但不构成写死意图。熔断本身是平台死物护栏，不替活人拿主意，合圣域。**唯一要守：别让"没手查证"变成"造假"**，见建议。
- **G-2 RC-B**：把"是不是活"从词表 `_是任务话` 手里交还给被点名本人判断——**正向修圣域**（意图归活人、词表退回快捷路）。不违背。
- **G-3**：锚句"以最新一句为准"是把当前意图判断权交还模型，不是平台替判（模型仍自己决定接旧话题还是新话题）。历史全量在场，未删留痕。合圣域。
- **G-4**：近音还原"老种"→"老钟"是**机械纠正输入噪声**，等价听清名字，不碰"这话啥意思/接不接"。合"机械识别归平台、意图归活人"。唯一分寸=近音只挂专有称呼、不挂职务词，已落实。
- **G-5**：只补守卫开关，不替活人判散会/接活，合圣域。
- **G-7**：三层墙钟都是"卡死必出声"的平台死物地板，不夺"跑不跑命令"的能力。落实"坏要有声音、不许假发生（panic 假报已停止）"。**正向修圣域**。
- **G-8**：落盘口过滤平台内部信号=场所只留真话，消除假气泡。合圣域。

**无一条把意图写死进代码。** 唯一软性风险是 G-1改动二"砍会议发言人的查证手"可能间接诱发造假，属能力设计问题而非圣域违背。

---

## ④ missed_cases（根因没钉死 / 漏分支 / 更深根因）

- **M1（G-1，重要）**：根因说"会议纯文本不计数→老钟反复开会烧钱"。改动一按"唤醒"计数确实堵总量。但**改动二关的是"会议内嵌套开会"，不是"老钟上层反复开会"**——老钟反复开会靠的是改动一的总量熔断兜。问题：G-1 自己说"改动一已覆盖上层反复开会，故不在会议室加轮次硬砍"。可 `_会议失控上限=30` 已在管单场；上层反复开会的真正根因是**老钟为什么会一轮轮开会**（是不是甲案/常识门让他反复被叫回统筹？）——G-1 只做总量兜底，没钉"为什么反复"。属"熔断兜底"而非"根治反复"，作者也明说是兜底，可接受，但别宣称根治了"反复开会"这个行为。

- **M2（G-5，漏分支）**：G-5 在 788 无条件 `if 办岗=="项目经理": 已转经理统筹=True`。但 788 会被**经理闲聊直通**也走到（经理被常识门以"日常聊天"叫醒、回了非空闲聊）。此时误置 True → 若同一句话后面真有同事 `[转办事]`，会命中 762 elif 被 continue 掉，**真活被吞**。修法漏了"只在经理这句确实是办了活/统筹时才置 True"。建议：把置 True 收窄到 `走办事底盘 and 办岗=="项目经理"`（走办事底盘=True 才是真接了活），别让经理的闲聊发言也点亮统筹开关。**这条不收窄=引入新吞活 bug。**

- **M3（G-2 RC-B，更深根因未覆盖）**：RC-B 让点名进办事底盘，解决"点名读文件掉回没 Read 的聊天底盘"。但**群聊冒泡路径（非点名）里，常识门以"日常聊天/地盘事"叫醒的人走的是 聊天唤醒(743)**，聊天底盘照样只有 search_hall_history/recent_hall_chats，**没有 Read/Grep**。即"不点名、只说'谁去读下X'"仍读不到磁盘文件——RC-B 只修了"@点名"这一条路，没修"群聊里非点名的读文件请求"。这条 G-2 没覆盖（也许超出本次范围），但根因"读不了自己文件"在非点名场景仍在。需知道边界。

- **M4（G-3，边界）**：`records[-30:]` 里若最后一条船主话之后又插了别人的话（同事回评/冷场），锚仍加在最新船主话上，同事后续话在锚之后——时序对。但若**本轮已说**(395) 里也有船主插话？不会，本轮已说是同事发言。安全。真正边界：一句话里船主连发两条(极少)，只锚最后一条，前一条降级为背景——符合预期。无漏。

- **M5（G-7，更深）**：G-7 三层墙钟到位，但 `run_command` 之外还有别的同步阻塞点吗？web_search/read_webpage（网络工具）若也用同步 requests 会同样冻 loop。G-7 只治了执行室，选做项给模型客户端加 httpx timeout。**网络工具集是否同步阻塞未审**——若是，同类根因还在别处。建议顺带核 网络工具.py 是否 async。属"根因类"未穷尽。

- **M6（G-4，漏）**：`_近音命中` 要求"等长、只差1字"。但语音转文字常见的是**多字/少字**（"老钟"听成"老中医"=多字，"项目经理"听成"经理"=少字）。G-4 只覆盖"等长错1字"，多字/少字近音不覆盖。作者未宣称覆盖这些，属已知边界，可接受。

---

## ⑤ apply_order / safe_auto / needs_owner_first

### 建议改动顺序（safe 先上、易冲突的靠后并肩改）
1. **G-8**（大厅记录.py 主改 + 办公室.py 833/846 辅）——最独立、最安全，先上。注意它的 846-848 编辑要与 G-7 协调（见 C1），所以 G-8 的大厅记录.py 部分可立即上，办公室.py 部分与 G-7 一起改。
2. **G-4**（常识门.py 单文件、纯增函数+一段替换）——独立，可安全直上。
3. **G-2**（RC-A 指路牌纯文本 + RC-B 办公室:673-680 独立段）——RC-A 可直上；RC-B 需船主认可"@闲聊也上办事底盘"这个取向（见下）。
4. **G-3**（活_本人.py 聊天底盘两处，独立于办事底盘）——需船主认可"最新一句为准"锚（见下），逻辑安全。
5. **G-5**（办公室:788 一行）——**必须先按 M2 收窄条件**再上，否则引入吞活 bug。收窄后安全。
6. **G-7 + G-1** 并肩最后改，因为两者都动 活_本人.py 730-815 热区与 办公室.py 841-867，且 G-1 删 809-815、G-7 加 740/744 超时会互相位移行号。**以同一份最新代码为基线，一次改完两条**。G-1 改动二（会议室降只发言）需船主先拍（见下）。

### safe_auto（可直接改、回归面极小）
- **G-8**（大厅记录.py 哨兵过滤 + 办公室辅助过滤）：机器控制字，误吞面≈0。
- **G-2 RC-A**（指路牌加绝对根提示）：纯文本、不碰权限工具。
- **G-7 必改一（执行室异步子进程）+ 必改二 line748 人名→_人**：治真根因、修真 bug，macOS 上 killpg 可用。变量改名要全（3处）。
- **G-4**（近音容错）：四重收口够严，误触实测小；唯一负担是维护静态表。

### needs_owner_first（设计敏感、船主先拍板）
- **G-1 改动一"200 唤醒硬上限"的新语义 + 会议计入总量池**：改后 200 含义从"调工具次数"变"唤醒次数(裁决+发言+每步动作全混)"，两三场会议就可能撞顶。**数值和"会议要不要计入总量"是船主该定的运营参数**，建议先 200 上真机跑一轮看会不会误掐正经会议，紧了提到 300。
- **G-1 改动二"参会人降只发言、失去 Read/Grep/web 查证手"**：这是能力裁剪，不只是关嵌套开会。船主要拍：会议发言人要不要保留查文件/查网页的手？**建议折中**——保留一个"会议发言但带资料室、不带 find_colleague/hold_meeting/assign_task"的模式（新参数如 `会议发言=True`：走协作模式工具集但砍掉找人/开会/派活三件），而不是一刀切成 `只发言=True` 把资料室也砍了。否则踩"逼人凭印象"红线。
- **G-2 RC-B"@任何话都上办事底盘"**：取向对（意图归活人），但船主要认可"@闲聊也吃办事底盘的重提示+成本"。低风险，但属产品取向。
- **G-3"最新一句为准"锚**：会轻微改变"续旧话题"的手感。作者论证不阻断续接，但这是船主实测抓的洞，改完的对话质感要船主亲验（G-3 自带的"你在吗"+"那dist弄好了没"两步验证必须船主看）。
- **G-5 收窄条件（M2）**：把置 True 收窄到 `走办事底盘 and 办岗=="项目经理"`——这是修法本身的必要修正，建议船主知情后按收窄版上。

### 一句话总纲
G-8/G-4/G-2RC-A/G-7(执行室+line748) 可安全直上；G-5 必须先按 M2 收窄再上；G-1 改动二（砍会议资料室）与 G-1 改动一（200 新语义）需船主先拍；G-7 与 G-8/G-1 在 办公室.py 841-867 及 活_本人.py 730-815 必须以同一基线并肩改，防行号漂移与 except 顺序错放。
