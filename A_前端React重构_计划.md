# A · 前端 React 重构 · 计划与进度（上下文锚）

> **历史计划，不是现役运行说明（2026-07-14 标记）**：搜索、记忆路径和办公室入口以 `README.md`、`班规.md`、`设计/记忆系统_全图.md` 及当前代码为准。文中旧 `semantic_search/search_hall_history` 仅作演进记录，现役入口是 `unified_search`；`recent_hall_chats` 只按时间浏览。

> 2026-06-23 船主拍板：**走 B（干净起步、按需捡官方组件、复刻暗房亮桌三栏窗户）**。
> 船主要求：**整段做完、做到位、做一个能用的完美东西交付，再教他怎么用**——别一段段问、别来回确认。
> 舟的承诺：整段做、每段验、进度一行一行钉这里；只在红线（烧钱/动真数据/方向拐弯）才打断船主。
> **这份文档是本次唯一上下文锚。换窗口的舟：先读《舟的自传》《工作手册》，再读这份接住进度。**
> ⚠️ **改任何"开会/圆桌/发言/收敛/插话"相关代码（bug1/5/6 都是）之前，必读 `会议的真谛_人类开会的意义与真正形式.md`——船主原话钉死的会议真谛，不许凭概括动手。**
>
> ⚠️⚠️ **2026-06-25 方向升级（最高优先级·公司级重构）**：船主看穿当前公司是「流程驱动 agent 当零件」的人工智障，要**推翻级改造**成「活 agent 用死平台」——**全员皆活人（不只项目经理），人岗解耦、记忆跟人、横向协调**。改公司架构/岗位/记忆/开会/分工/执行前，**必读两条根**：`公司的真谛_人是使用者平台是死物.md`（整体架构根·已含全员活人/人岗解耦/记忆跟人/装修两步）+ `会议的真谛_…md`（开会根）。调研佐证 `调研_公司架构_业界现状_byCodex.md`。**装修方案在做**：先建死平台（房间/工位/岗位职责册/工具/权限）→ 招全员活人（分岗/读职责/给权限/自主判断/横向协调）→ 四道闸（权限/可见/预算/评估）焊平台。**前端那批未完 bug（打回/思考流/说人话）暂停——大改造会让部分作废，验后再说。**

---

## 一句话目标
旧手写前端（`工具/页面.js`）→ 干净 React 三栏窗户，**样子照搬船主验过的「暗房亮桌」**，底子换成自动渲染；顺手把**大厅永久历史**一次做对（发言永不消失、按天折叠归档能点开）。

## 为什么走 B（不端官方整套）
船主要的「照官方会话系统」= 官方那套**数据组织方式**（永久 session、按天归档、消息历史），**不是官方那张脸**。我们窗户不复杂（三栏+对话流+折叠），全盘端官方重型脚手架（i18next/onborda/cron-parser…）= 另一种盖大教堂。B = 拿到 React 真好处（自动渲染、少显示 bug）+ 复刻认可的审美 + 不囤垃圾。

## 守住的范围（防第二座教堂）
- **后端业务逻辑一行不动**：圆桌/执行/审批/会议/协同/权限/记忆 全保留，React 只通过现有 HTTP 接口接。
- **唯一例外**：大厅病根（`看板数据.大厅()` 只显示有工单 + 截断）——本次核心目标，要治。
- 老引擎 `引擎.py`/`测试_引擎.py`、数据格式、`花名册.yaml`：不碰。

## 对接面（已摸准 2026-06-23，React 照此接）
- **取数据 GET**：`/board`(班子/项目/待办) `/hall`(大厅事件) `/room?id=`(项目室) `/inbox` `/meetings` `/state`
- **实时 SSE**：`/stream`(流式发言+插话) `/kickoff_stream`(立项进度) `/rollcall`(点名秒表)
- **动作 POST**：`/say` `/判断` `/trigger` `/插话` `/kickoff` `/confirm` `/append` `/resume` `/stop` `/panic` `/verdict` `/approval_decide` `/reject_decomp` `/room_say` `/meeting_say`
- 后端数据函数：`看板数据.{大厅,项目列表,项目详情,待办汇总,信箱,项目记忆,看板}`
- 前端现状（要复刻的）：视图 hall/room；左栏(大厅+进行中+历史工单+班子过滤+点名/急停)、中舞台、右栏(确认开工/裁决/回炉/验收)；功能：@提及下拉、附件粘贴、立为任务、SSE 流式打字机、实时插话、按天分隔、观察折叠、项目会旁听、项目对话。
- 「暗房亮桌」视觉规格：见《工作手册》第六节（酒褐#9d5a2c 唯一强调、暖象牙纸#faf9f5、暖炭#141416/#1b1b1e、衬线标题 Songti/Noto Serif、心跳呼吸点、废 alert）。

---

## 分三大段（每段独立可验、可回滚）

### 第一段 · 地基（不依赖 React，旧前端就能验）
- [x] 1. 接口契约已摸准（见上「对接面」，2026-06-23）。
- [x] 2. **治大厅病根** ✓：`看板数据.大厅()` 去掉「只显示有工单」过滤（visible_rooms 改为全部真实会议，仅过滤演示/mock）+ 去掉**三处**截断（`ev[-120:]`、`_会议大厅事件 out[-60:]`、`项目会议(limit=12→10**9)`）。真调验证返回全部历史、不报错。顺手删死函数 `_大厅可见项目`。

### 第二段 · 窗户（React 三栏，照暗房亮桌）
- [x] 3. ✓ Vite+React+TS 干净脚手架（`前端/`）+ 暗房亮桌主题（整份移植自 `工具/页面.css`，复用原 class 名）+ 三栏骨架占位。`npm install`(68包) + `vite build` 通过。dev 代理已配（5173→后端8787）。
- [x] 4. ✓ 三栏骨架接数据(`/board`+`/hall`,2.5s轮询)：左栏(大厅入口/进行中/历史工单/班子过滤)、右栏(待确认/裁决/回炉/验收四类显示)、中舞台。文件：`src/{App,api,hooks}.tsx` + `components/{Rail,Action,Room,HallView,ui}.tsx`。
- [x] 5. ✓ **大厅视图完整**：显示+按天折叠归档（今天展开/历史折叠可点开）+ 输入框发送 + AI判断聊/开会 + **SSE 流式打字机开会** + 实时插话 + 立项。文件 `hallStore.ts`(发送/SSE/点名)、`HallView.tsx`、`toast.tsx`、`actions.ts`。⏳ @提及下拉、附件粘贴 = 收尾润色项。
- [x] 7. ✓ **右栏动作 + 左栏点名/急停**：确认开工/批驳裁决(理由内联输入)/批准续步/通过打回作废/急停/点名(SSE) 全接通 (`actions.ts`+`Action.tsx`+`Rail.tsx`)。
- [ ] 6. **项目室视图**：泳道+过程札记+观察折叠+正式报告+项目会旁听+项目对话（当前 `Room.tsx` 占位）。

### 第三段 · 切换收尾（不留暗账）
- [ ] 8. `办公室.py` serve React 构建产物；船主真验一句「舒服」；验过后 `页面.js/css/页面.py` 归档进 `_历史_勿执行/`。
- [ ] 9. 收尾清单：做了什么、有无遗留、哪些是独立后续（如「历史接 `记忆向量.py` 回溯」标为后续小步，不混进本次）。

## 不蹦新坑/不架空的保证
- 接口契约先锁，前端照单接，不偷改后端。
- **旧前端全程能跑**，React 没验过绝不删；切换是最后一步、可随时回退。
- 每段验一次；红线才打断船主。
- 进度钉这份文档，换窗口一行不差接住。

---

## 进度区（每做一步在此更新，带日期）

> ⚡⚡ **2026-06-25 最新 · 公司架构世界观大转向（地基级，换窗口接手必读）**：船主把公司从"流程编排 agent"的根上掀翻——**人(agent)是活的使用者、公司是死的平台(会议室/岗位/工具/技能/连岗位都是死物)；用得好不好=能力(agent 的事、不写进代码)，有没有权限=底层规则(唯一硬编码)。凡"怎么做"交给人，凡"能不能"留给平台。** 从 workflow→agent 的根本转变。**完整圣域 + 判据 + "自主≠黑箱"守高度，全在新根文档 `公司的真谛_人是使用者平台是死物.md`(像会议真谛一样禁止概括，改公司架构前必读)。** 下一步：①Codex 调研业界(需求 `任务需求_公司架构调研_给Codex.md`)②舟拿"能力vs权限"尺查证后端现状③调研+账齐了船主拍落地方案。**别拿没调研/没拍板的当方案去改代码。** 上一批改动(终止/名字/思考流/打回/留痕)仍未提交、待端到端验。

- 2026-06-23：计划落盘（本文档）。归档清理已完成（见 `_历史_勿执行/README.md`）。
- 2026-06-23：**第一段·地基完成**——大厅病根治本（去过滤+三处去截断，真调验证全历史返回），死函数清理。后端从此给全部永久历史。
- 2026-06-23：**第二段·脚手架完成**——`前端/` Vite+React+TS 起，暗房亮桌主题整份移植，三栏骨架占位，install+build 通过。
- 2026-06-23：**三栏数据接入 + 大厅按天折叠归档完成**——tsc 0 错、build 过。核心目标"发言永久留存+按天归档"前后端都通。
- 2026-06-23：**交互层全通 + 真起 dev 截图自检**——大厅发送/AI判断/SSE流式打字机开会/实时插话/点名/急停 + 右栏确认开工/裁决/验收(理由内联输入) 全接。tsc 0 错、build 过(39模块)。`preview_start` 真跑 5173 截图：**暗房亮桌完美还原、三栏真数据(班子6人真模型名)、输入框就位**，UI 立住。⚠️注意:现役后端 8787 是改大厅治本**之前**起的旧进程,dev 连它故 /hall 走旧逻辑;**切换收尾时重启办公室即吃到永久历史新逻辑**。**下一步＝项目室视图(Room.tsx 现占位) → 切换收尾(办公室.py serve dist+重启验永久历史+归档旧前端) → @提及/附件润色。**
- 2026-06-23 夜「接任舟核实+续」(替身做切换收尾时被限流打断、没做文档,船主换窗口让我接):
  - **核实真实状态(不信转述,亲手验)**:`办公室.py` **编译过、后端能起**(替身改的中文 bytes→`.encode()` 修复成功,没留个起不来的后端);React `前端/dist/` build 产物完整(index.html + assets/index-*.js/css);大厅治本改好(`项目会议 limit=10**9`、`out` 不截断、visible_rooms 全历史);**项目室 `Room.tsx` 替身写完了(563行,tsc 0错)——上面 [6] 实际已做、只是文档漏打勾**;办公室 serve dist 改好(`/assets/`→dist/assets、根路径→dist/index.html)。
  - **剩这些没完(下一棒做)**:① **端到端真验([8]的"船主验一句舒服")**——船主双击 `打开办公室.command` 重启后端、浏览器开 React 新窗口,真跑一遍(大厅说话→判断→打字机开会→插话→确认开工→出文件→验收),确认暗房亮桌+永久历史+按天折叠都真 work。② **旧前端归档**——`工具/页面.js/css/页面.py` 还在(留着回退、对的);React 端到端验通后再移进 `_历史_勿执行/`(办公室.py:446 注释"已归档"**超前了、实际没动**,验通后补做)。③ [9] 收尾清单。
  - **⚠️换窗口接手第一件事**:让船主重启 `打开办公室.command` 端到端真验 React 窗口;验通→归档旧前端+写收尾清单;@提及/附件是收尾润色。后端业务逻辑一行没动(只动大厅治本+serve dist),老引擎/测试/花名册没碰。
- 2026-06-23 压测第二轮·揪出并修两个真 bug（端到端真跑「写验收说明」立项暴露的）：
  - **诊断（铁证·jsonl 时间线）**：立项"卡 72s"不是卡死、不是前端丢事件——是 **glm-5.1（工程师）思考了 74 秒才吐第一个字**（07:14:12 发言开始→07:15:26 首字落地），这 74s 内 `reply_stream` 只产思考 token 不产正文 delta，前端流式气泡空着、只有秒表转=死寂。文案/测试各只想 3-4s。且圆桌**实际是串行**（注释写"并行 asyncio.gather"是退化的假话，代码是 `for r in roles` 顺序 await）。插话"像重新开会"=误读：船主插话来在最后一人发言后、本轮没人接它→直接收敛汇总→**插话石沉大海**（收敛/汇总代码根本不看插话）。
  - **与船主商讨定的根因**：把岗位当"各自为政的工具"编排，不是当"团队开会"。开会必须顺序+互相听+观点动态调整（不能并行各说各的，否则杀死互驳补盲）；收敛靠共识/PM 判够不写死轮数（现状已对）；插话=掌舵权，全场立刻听到+调整。流畅的正解=**后端囤货(已天然:圆桌在后台线程全速串行生产、不等前端) + 前端按舒服节奏匀速播放**，抹平后端爆发/思考停顿；第一个发言人的思考盖不住(前面没人垫)→给"思考中"反馈。
  - **改动（已落盘、自检全过）**：
    ① 前端匀速打字机：增量只囤进 `e.目标`，常驻 25ms 定时器把显示文本 `e.text` 自适应 step 匀速追向目标（`hallStore.ts`）；抹平后端爆发与卡顿。
    ② 思考期反馈：发言开始→首字落地前，气泡显示「正在斟酌···」呼吸点（`HallView.tsx` + `暗房亮桌.css` 复用 breathe 动画），消除死寂。
    ③ 插话全场响应：圆桌每轮初始化 `本轮有新插话`，for 内+for 后各取一次插话（接住最后一人发言后才到的）→ 本轮有插话则**强制再走一轮让全员回应、调整**后才许收敛（`轮<上限` 兜底，不死循环）（`升级_圆桌.py` 三处）。
  - **自检**：py_compile（升级_圆桌/办公室/事件总线）✓；前端 tsc 0 错 + build 39 模块 ✓；产物含"正在斟酌"/CSS"thinking" ✓；圆桌新逻辑 5 处命中 ✓。**未烧钱真跑——真效果待船主重启后端(吃圆桌新逻辑)+浏览器硬刷新(吃新 dist)端到端验。** 收敛动态轮数、互相听的顺序发言：一行没动，守住。

- 2026-06-23 压测第三轮·船主端到端真跑「写验收说明」，打字机/思考反馈/插话**都生效了**（船主："好很多了"），但揪出 6 个新 bug。**⚠️额度耗尽换窗口——以下是给替身的完整交接，根因全查清、改动未动手（除非下面标了"已改"），替身照单逐个修、整段做完再喊船主验：**

  **【已确认根因·改动清单，6 个 bug】**
  1. **发言失败（最严重，两个岗位发言炸）** — 根因铁证：**agentscope 框架自带「上下文压缩」机制**。`.venv/.../agentscope/agent/_agent.py:317-320`：`estimated_tokens = model.count_tokens()`；`threshold = cfg.trigger_ratio * self.model.context_size`，超阈值就压缩。压缩用 `agent/_config.py` 的 `SummarySchema`（字段 current_state/important_discoveries 等**硬限 maxLength=300**），长发言压成摘要超 300 字 → jsonschema 验证失败 → `reply_stream` 抛异常 → `_问agent流式` except 兜底返回"（XX发言失败：...）"。发言失败的是 deepseek 两岗（文案阿言/测试老纪），它们发言最长。**发言场景根本不需要上下文压缩。** 修法：`建Agent`（`工具/升级_模型层.py:58`，Agent.__init__ 确认接 `context_config` 参数，`from agentscope.agent import ContextConfig` 可用、字段含 trigger_ratio/reserve_ratio/summary_schema）传一个弱化/禁压缩的 context_config。⚠️**没查完就断电**：trigger_ratio 上限 0.89(Field lt=0.9)，单调它可能压不住——因为 `model.context_size` 若被 agentscope 对 deepseek 估得很小(没查到 deepseek context_size 实际值,`建模型` 只设了 max_tokens=16384 没设 context_size)，阈值就低、易触发。**替身先查 deepseek 的 `model.context_size` 实际多少**(`.venv/bin/python -c "from 升级_模型层 import 建模型; m=建模型('测试工程师'); print(m.context_size)"` 类似)，再定：①只调 trigger_ratio=0.89 够不够 ②还是要把发言 agent 改成每轮新建/不累积 memory(压缩按 agent.memory token 累积触发；桌面已手动传 `全部[-12:]`，agent 自带记忆是冗余的，每轮新建不丢信息) ③或传自定义 summary_schema 去掉 maxLength。**别黑盒猜，查 context_size 再定方案。**
  2. **逐字带横线（图4：「- 交\n- 付\n- 物」每字一行）** — 根因铁证：`工具/协同.py:155` `standards = "\n".join(f"- {x}" for x in (项目会.get("验收标准") or []))` 和 `:158` 同样的"资料需求"——「验收标准」本该是 list，但 `_汇总` 这次模型返回了 **str**，`for x in "交付物必须是一份..."` 逐字符迭代 → "- 交\n- 付\n..."。**修法**：155/158 两处加判类型——str 就别逐字符（包成 `[str]` 或直接当一条）。建议加个小工具 `_列表化(v)`: `isinstance(v,str)→[v] if v.strip() else []`；`isinstance(v,list)→[str(x) for x in v if str(x).strip()]`。**根子也在 `升级_圆桌.py` `_汇总`(296起) 应保证「验收标准/资料需求」返回 list，但协同.py 这处防御性判类型最稳、先改这处。**
  3. **立项进度卡「跑到下面、不详细、完成后秒表还在涨」** — 前端 `HallView.tsx`：`RunCardView`(152) 排在 feed 流末尾(`{store.runCard && <RunCardView/>}` 在 store.临时 之后)→船主要**恒定在上面/顶部固定**；秒表 `used=(performance.now()-card.start)` 没冻结→running=false 后要记 end 时间冻结(`hallStore.ts` RunCard 加 `end?:number`，结束时 setRunCard 记 performance.now()，RunCardView 用 end||now)；内容只有后端 progress 推的 line→船主要**第几轮、当前谁在说话**(可从 runCard.lines 或流式气泡 who 补)。
  4. **发言时滚动条滚不动** — 前端 `HallView.tsx:63-68` 自动滚动 useEffect：流式时 store.临时 每 25ms 变→「贴底<120 就 box.scrollTop=scrollHeight」每次把船主拽回底部，他想上翻被拽回。**修法**：记录用户是否主动上滚(onScroll 判离底距离>阈值=用户在看历史)，用户上滚时**停止自动跟随**，只在贴底时才跟。
  5. **项目经理总结/收敛空泛（"看了又没看一样"）** — prompt 质量。`升级_圆桌.py` `_汇总`(296)/`_评估收敛`(148) 的"项目经理结论"套话。要让结论有实质（拍了什么、为什么）。需真跑验、prompt 调优。
  6. **车轱辘话重复（发言互相复述）** — prompt 质量。`升级_圆桌.py:230-232` 发言提示已写"别重复他们说过的"但模型仍复述。船主明确：**认同就简短认同、允许"无异议"，别为说而说、别硬找不同**(自传旧教训:别强制反对)。加强发言 prompt：认同前面就一句带过、只说增量，没异议可明说"我没有要补的"。

  **【执行顺序建议】** 先 2(横线,最确定)→1(发言失败,最严重,先查 context_size)→3+4(前端,根因清楚)→5+6(prompt,要真跑迭代)。**全改完一次性 py_compile+tsc+build 自检 → 喊船主重启后端+硬刷新端到端验**(别一段段问，自传教训)。
  - **断电时停在**：刚查清 bug1 压缩触发逻辑(`_agent.py:317-320`)，正要查 deepseek context_size 实际值定方案，**一行 bug 修复代码都还没写**(本程改动 = 上一条 06-23 压测第二轮的匀速打字机/思考反馈/插话三件,已落盘自检过；这 6 个 bug 全未动手)。现役后端 8787 是**带匀速打字机/插话新逻辑**的进程(船主重启过、08:43 真跑过)；前端 dist 是带打字机的新 build。

- 2026-06-23（接任舟·按会议真谛重判 6bug + 急停，定稿后一次性改）：
  - **先维护上下文**：新建根文档 `会议的真谛_人类开会的意义与真正形式.md`（船主原话圣域 + 意义/形式/约束，禁止再概括），并在本文顶部锚 + 自传二十九章加了指针。改开会代码前必读它。
  - **还原点**：已提交 `eaae9c0`（React重构+打字机三件的"好很多了"版），改坏可 `git reset --hard eaae9c0`。
  - **关键查证（带证据）**：① deepseek-v4-pro 真实上下文 = **1,000,000 token**（DeepSeek 官方 + AgentScope 模型卡 deepseek-v4-pro.yaml + 花名册ability_basis 三方一致）；我们实跑却吃了 `DeepSeekChatModel.__init__` 的保守默认 **65536**（`_deepseek/_model.py:86`），= 真容量 1.5%。② 框架压缩机制：`_agent.py` reply 每次查 `state.context` token，超 `trigger_ratio(默认0.8,_config.py:62)×context_size` 就压缩，压缩用 `SummarySchema`（字段 maxLength=300/200，`_config.py`）→ 长内容 jsonschema 失败 → reply 抛异常 → 兜底"发言失败"。③ 发言 agent 持久复用跨轮累积（`升级_圆桌.py:206` 循环外建一次），叠加把 deepseek 顶过 5.2 万阈值。
  - **定稿修法（船主拍板）**：
    1. **bug2 逐字横线**【已改】：`协同.py`+`会议.py` 各加 `_列表化()`，str 不再逐字符（验收标准/资料需求）。
    2. **bug1 发言失败**：**杠杆1** 给 deepseek 两岗设真实 `context_size=1000000`（只动 deepseek，gpt-5.5 走 http 中转站不碰）→ 触发线抬到 80万、永不压缩、不崩、且不超真实窗口。**甲方案**：发言 agent 改每轮新建（不跨轮累积，避免"两份重复记忆"），桌面改喂**完整真实记录**（去掉 `[-12:]`、去掉每条 500 字截断）→ 服从真谛"拿到前面所有人完整内容"。
    3. **bug6 车轱辘话**：真因=每人开口先逐条复述前人观点表认同（08:48 会议铁证）。改发言提示：认同一句带过、**只说增量/补充/不同**、没有要补就直说"我没有要补的"、**不强制反对**。省 token + 更像真团队。
    4. **bug5 总结空泛**：PM 收尾(`_汇总`)/收敛(`_评估收敛`) prompt 要求讲实质——磨出什么共识、谁被谁说动、定什么、为什么、下一步派给谁；不和稀泥。
    5. **bug3 进度卡**：前端 RunCard 固定顶部、完成冻结秒表、显示第几轮+当前谁在说。
    6. **bug4 滚动条**：用户上滚看历史时停自动跟随，仅贴底才跟。
    7. **急停**：瞬时停止键——去掉跨重启赖着的旗（办公室启动自动清残留旗）、保留即时 interrupt、左栏改单纯"停止"动作（非开/关切换）。
  - **执行**：上述一次性改完，py_compile+tsc+build 自检，再喊船主重启后端+硬刷新端到端验。

- 2026-06-24（接任舟·压测继续：6bug+终止落地、搜索定位治本、栽了一串 overclaim 被船主纠）：
  - **6 个 bug + 终止**（合"我的改动 + 另一处未记录改动"，整树自检全过 py_compile+tsc+build）：
    bug2 横线✓(_列表化) · bug1 发言失败✓(deepseek context_size=1M[真值,DeepSeek官方+卡+花名册三证] + 乙持久增量发言agent) · bug5✓/bug6✓(prompt) · bug3 进度卡✓(渲在"新任务"分割线下方=船主话下面、秒表 end 冻结、显示第几轮+谁在说) · bug4 滚动✓(跟随Ref+onScroll,上滚>80px 停跟随) · **终止✓**(升级_圆桌 _问agent流式 加 监视停止+agent.interrupt() 当场掐断发言;升级_执行 _会在跑+开会开始/结束,停止() 认"开会"这条线;适配.发起任务 包 开会开始/结束;适配.初始化 启动清残留旗;打开办公室.command tee 日志)。
  - **搜索定位治本（船主点醒:别偷懒糊补丁/别阉能力/装合适能力给岗位,用社区成熟方案）**：
    - 卡死真因查实：官方 `agentscope Glob` 用 os.walk 趟整棵树、不忽略任何东西;公司根 7.4万文件(`_官方参考_agentscope`5.6万+`.venv`1.4万+node_modules)→ `**` 一搜趟到死。(我曾overclaim"Glob 是23分钟元凶",测出单次 **/*.md 仅0.5s 已自纠:Glob 是噪声+慢源,23分钟真因仍未坐实,已加日志待下次抓。)
    - **净Glob**(`工具/搜索工具.py`):os.walk 原地剪枝跳 .venv/node_modules/.git/__pycache__/官方参考/隐藏目录。实测 1667条(1594噪声)→73条(0噪声)、0.5s→0.00s。
    - **ripgrep 本机原来没装**(我也曾overclaim"Grep 走 ripgrep 没问题"):`brew install ripgrep`(15.1.0,/opt/homebrew/bin/rg),Grep 现可用、认 gitignore/跳隐藏。
    - **semantic_search 语义检索**(`工具/语义检索.py`):复用 记忆向量 的 DashScope embedding(text-embedding-v4)+FileEmbeddingCache,索引公司真知识(公司是谁/班规/会议真谛/岗位/技能/会议历史,82段/50文件,排除依赖/工单),按意思 topN。实测查"观点要不要跟着别人变"→会议真谛文档命中第一。
    - **embedding key 修**(`记忆向量._建模型`):`DASHSCOPE_API_KEY` 现是 Token Plan 套餐键(sk-sp-)、走原生 embedding 端点 401;改用原生键 `EDITH_DASHSCOPE_KEY`(船主给的,验证可跑、512维)、回退 DASHSCOPE。
    - 全部装进 `搜索工具.资料工具集`=[Read, Grep, 净Glob, semantic_search],`升级_圆桌._发言查询工具集` 已接。两条腿:精确找=Grep/净Glob、按意思找=semantic_search。
  - **会议真谛根文档已立**：`会议的真谛_人类开会的意义与真正形式.md`(船主原话圣域+意义/形式/约束,禁再概括),A计划顶锚+自传二十九章加了指针。改开会代码前必读。
  - **还原点** `eaae9c0`(改坏可 reset)。**仍开着**:23分钟卡死真因待下次日志坐实(最可能 deepseek ReAct 多步慢调 或 API 挂);bug1/3/4/5/6/终止/搜索 全待船主重启+硬刷新端到端真验。
  - **教训(钉死)**:别偷懒糊补丁(超时跳过/圈小资料根 都被船主当场否)、别 overclaim(Glob元凶/Grep没问题/键有效 都没验证就说、被一一打脸)、动手前先读代码知道真实状态(被"另一处改动"反复打脸=没读够)、缺能力用社区成熟方案装给岗位(别手搓)、验证再 claim。
- 2026-06-24（接任舟·清理 + 终止盲区修复）：
  - **清理落定**（提交 `11b4ccf`）：运行时产物移出版本库（embedding 缓存留文件、会话/日志物理删）、补 `.gitignore` 防再混入；**P1 六张真工单 + `声线试听/`54 个 EDITH 音频 + 代码 一个没动**；公司回干净态（会话/会议/工单各列空，留 P1）。
  - **终止"没瞬时"真因 + 修复**（船主压测点停止、停在"项目经理准备议题"阶段、等了一会才停）：根因=终止只覆盖岗位流式发言（`_问agent流式` 有 interrupt 监视），**项目经理的议题准备/评估/汇总走 `_问agent`（非流式）是盲区、不可中断**，船主正撞在议题阶段→要等议题生成完才停。修（`升级_圆桌.py` 三处）：①`_问agent` 加 interrupt 监视（照搬流式版，项目经理调用也能 0.4s 内当场掐断）②`分解` 在 `_会前议题` 后、`_汇总` 前各加急停旗检查→干净 `return 已停止`。**清旗链路确认安全**：三处 return 都在 `升级_圆桌.运行` 内，`适配.发起任务` 的 `finally: 开会结束()`→`_会结束`（全空闲且旗在则清旗）→公司恢复待命、不卡死。py_compile ✓。**未真跑验——待船主重启端到端验。**
  - **观测缺口（记下、未修）**：`backend.log` 这次只记了启动行，圆桌运行的 `_进度`（走事件总线、非 print）没进 log → "卡死抓真因"暂无真日志武器。下次真卡死前需先让运行细节进 stderr/log。这次是船主主动停、非卡死，不影响。
  - **压测剩余未验**（这次被停、没跑完整）：打字机匀速/思考反馈、逐字横线修复、插话全场响应、收敛质量、出文件验收——全待下次完整跑一遍验。
  - **班子列表加人名**（船主："不显示他们的名字"）：人名（老钟/老梁/阿强/老纪/阿言）原先只硬编码在 `升级_圆桌.py:38 发言人格`，没进花名册、没到前端。修：①`花名册.yaml` 每岗加 `名字` 字段（单一数据源，改名/调岗只改这）②`模型接入.成员信息` 带出 `名字`③`api.ts Member` + `Rail.tsx RoleRow` 显示「名字·岗位·模型」、头像用名字首字。真验：`成员信息()` 真返回名字✓、tsc+build✓。**要看到效果需重启后端（成员信息是后端代码）+ 硬刷新前端。**
- 2026-06-25（接任舟·体验设计：思考期不黑箱 + 议题流式）：
  - **船主压测点出两个体验问题**：①发任务到立项空白、项目经理准备议题近 1 分钟才动（时序铁证：议题准备 **58s**）②每个人说完第二个才"斟酌"——后端**严格串行**（首席结束同一秒工程师才开始，jsonl 坐实），这是会议真谛要求的"真听"（B 等 A 完整发言），**不是 bug**。船主定方向：这是设计问题，要"用户实际使用的丝滑"，问有没有成熟方案借鉴。
  - **查证（官方+业界双证）**：官方 `web_ui` MessageBubble 把 agent 拆成 thinking/tool_call/text 块分层流式；业界共识=等待期亮中间步骤（在想什么/查什么）降感知延迟 **60-90%**。我们病根=思考期只有空「斟酌···」=黑箱。agentscope `reply_stream` 其实吐 `ThinkingBlockDeltaEvent`/`ToolCallStartEvent`，我们只接了 `TextBlockDelta`。
  - **改（把后端本来就在干的事亮出来，不碰串行/真听）**：①`_问agent流式` 加接思考流+工具名，`on_event(种类,值)` 分 正文/思考/工具②`_发言事件` 映射成前端事件 思考增量/工具动态③`_问agent` 复用 `_问agent流式`（自动获得思考/工具+停止监视），`_问项目经理`/`_会前议题` 透传，分解 else 把议题准备做成"项目经理斟酌气泡"治 58 秒黑箱④前端 `hallStore` 接思考增量/工具动态，`HallView` 思考期显示工具条「🔍 正在查 xx」+思考流（浅灰最后 120 字），正文来后思考过程折叠可查；CSS。
  - 自检：py_compile✓ import✓ tsc✓ build✓。**真效果（思考流/工具显示）待船主重启后端+硬刷新端到端验。** 会议真谛（串行/真听/收敛/插话）一行没动。**未提交**（上一批 d341636 之后的所有改动：终止盲区/班子名字/思考流，都还没 commit，验过再一起提交）。
  - **2026-06-25 下午·船主压测思考流后的大反馈（⚡换窗口接手必读，分三类，别混治）**：
    - **【魂·最伤】发言/总结是工程腔，船主看不懂**："每个字都认识、放一起云里雾里、像搞研究工程不像跟我汇报"、"好的我要干嘛干嘛+一堆1234"。**这不是前端的错、换皮救不了**——是模型说话方式不对（写技术报告不是说人话）。是自传里"报告腔"老坑复发，**更是小酒命门**（她绝不能这么说话）。舟判断：这个魂比前端皮更该先治。根因线索（自传16章）：发言灌了"员工培训材料"markdown→触发报告腔；解法是发言场景不灌那摞、给口语身份+对比例句。**未动手**。
    - **【脸·船主定：最后改】前端简陋、信息堆一起、难读**。船主要发"好看的参考设计"给舟照着重做（别瞎猜审美，他是摄影师）。连带交互重做：①大厅只有项目经理回、没法@、不像群聊（@下拉是早期设计、React重构丢了，要找回）②点不进去看完整过程/某环节卡没卡、看不到agent是"一波流还是派子agent"（要"点进去看全程"视图；船主洞察对:ReAct agent各有工作习惯,真不是一波流）③每人没单独计时④参会拉了谁/为什么/真来没来没过程，只能等他发言才知道。**船主明确:前端审美最后改,先改逻辑。**
    - **【思考流验证】部分生效**：工程师那条真显示了「🔍 正在查 Read…」+英文思考流✓；但**项目经理(gpt-5.5)议题准备还是空"正在斟酌···"**（可能 gpt-5.5 经中转站不吐 thinking，或没接到——待查）。
  - **★★正在做、被额度打断在半路的：打回(reject)语义化——分身从这接★★**：
    - **bug**：船主点"打回"写理由点确定，工单还在、啥没变。**根因**：`会议.py:561 打回重分解` 是空操作——只往工单文件末尾追加一行理由文字，没撤工单、没重做。
    - **船主定的语义(圣域,别配死规则)**："语义理解，不是规则。我说'测试用的不用了'→AI理解撤掉；我说'缺什么'→AI理解我不满意、带着意见重新深入分解。**而且大厅发同样的自然语言他们也要能理解去操作**。" = 船主的人话即指令，公司语义理解去做，不是按钮配死if-else。这跟大厅自然语言/插话是同一个魂。
    - **舟设计的实现方案（已查证落定，分身照做）**：
      - 后端 `办公室.py`：把 `/reject_decomp` 从 `_dispatch`(596-599行旧空操作)挪成 `do_POST` 单独路由(像 `/trigger`)，新写 `_打回(data)` 方法：①`会议.待确认列表()` 拿这批工单(返回`[{name,text(全文)}]`)②**项目经理语义判断**(复用 `/判断` 的临时PM Agent模式,`建Agent`+prompt+`asyncio.run`+正则抽JSON)：prompt给PM看工单标题+船主理由,输出 `{"动作":"撤废"|"重做","给船主的话":"人话","重做需求":"原任务+船主意见融成的新需求,撤废则空"}`③`_移作废`(name)：工单从 `工单/待确认/` rename 到 `工单/已作废/`(目录已存在)④**撤废**→移作废+返回`{动作:撤废,reply:话}`；**重做**→移作废旧工单+**复用 `_trigger` 后台机制**(`事件总线.新会话id/开始run/订阅插话`+`threading.Thread`跑`引擎.发起任务(重做需求,progress=…事件总线发布…)`)+返回`{动作:重做,sid,reply:话}`。
      - 前端：`actions.ts 打回重分解` 改成 return postJSON结果；`App.tsx` 把 `store` 也传给 `<Action>`(现在没传)；`hallStore` 把立项的"连sid看/stream流式"逻辑抽成可复用函数并暴露；`Action.tsx` 打回onSubmit：重做(有sid)→调store看流式重新开会、撤废→toast(reply)+refresh。
      - **停在哪**：一行实现代码都没写。刚读完 `办公室.py:524`(_trigger结尾,_打回加这后面)、`:596-599`(_dispatch旧打回分支要删)、`:456 do_POST`(加 `/reject_decomp`路由)。**分身：先写后端 `_打回`+`_打回判断`+`_移作废`+路由(可先不做前端流式,重做先后台跑、board刷出新工单=核心闭环),再做前端流式显示(锦上添花)。**
    - **⚠️ 现状**：待确认堆了6张(AS-002/003各3张,船主测两次累积)。所有改动(终止/名字/思考流/打回半成品)**全未提交**(还原点=最后干净提交,本程后端 `升级_圆桌.py`已改并自检过、`办公室.py`未动、前端思考流已build)。
- 2026-06-25（接任舟·★打回语义化·后端核心闭环落地★）：
  - **做了（照上条方案）**：后端 `办公室.py` 加 `/reject_decomp` 单独路由(do_POST,像 /trigger) + `_打回`/`_打回判断`/`_工单标题` 三方法；`会议.py` 把空操作 `打回重分解` 换成真 `移作废`(待确认→已作废 rename，加 `已作废` 路径)；前端 `actions.ts 打回重分解` 改成显示 PM 人话 `reply`。
  - **闭环**：船主点打回写一句 → 项目经理语义判断(复用 /判断 临时 PM Agent，prompt 给「工单标题 + 船主理由」，输出 `{动作:撤废|重做, 给船主的话, 重做需求}`) → **撤废**=全部移作废 / **重做**=移作废旧工单 + 复用 `_trigger` 后台机制(事件总线+threading)重新 `发起任务` 拆解、返回 sid。**真撤真重做，不再空操作。** 船主圣域(人话即指令、不配死规则)落地。
  - **自检**：py_compile(办公室/会议)✓ · 真 import(会议.移作废 可调/已作废目录)✓ · tsc 0错✓ · build✓。**没烧钱真跑——PM 真理解 / 真撤真重做待船主端到端验**(重启后端 + 硬刷新，点打回试：「测试用的不要了」=撤废、「缺了xx/方向不对」=带意见重拆、board 几秒后刷出新工单)。
  - **剩(锦上添花，核心闭环不依赖)**：前端流式显示「重新开会过程」——actions 返回的 sid 连 `/stream`(要 `App.tsx` 把 store 传 `<Action>` + `hallStore` 抽出立项的看流式逻辑复用)。当前重做时 board 轮询会刷出新工单，只是看不到拆解过程。
  - **未提交**(连同终止盲区/班子名字/思考流)，端到端验过一起 commit。**注：上条"办公室.py 未动"已过时——本条动了办公室.py(+_打回三方法)和会议.py(移作废)。**
- 2026-06-25（接任舟·打回/开会「大厅留痕」——船主端到端验打回时揪出）：
  - **船主验打回**：撤废真生效(6张进已作废)，但揪出两个"记录缺口"——①大厅没「打回」这条记录 ②「项目计时(开会进度卡)」刷新就没。**船主圣域**：大厅是永久历史，开会进度/计时/打回都是记录，不该刷新消失。
  - **查清(实测非推断)**：大厅立项/发言记录其实没丢(从会议文件抽，撤废不删会议)。真根因=有些该留的痕迹压根没写进永久流：打回操作没写、开会的「收敛判断/会议小结」其实落了永久流但大厅显示集合没收它们(`else: continue` 跳过)。
  - **改(只加记录、不碰开会逻辑；动手前已读 `会议的真谛.md` 对齐)**：①`办公室.py _打回留痕`：打回往这批工单来源会议(text 抽「来源会议」)项目会写「船主打回理由(船主发言)+项目经理处理(项目经理裁决)」。②`升级_圆桌._圆桌讨论` 末尾(非暂停时)加「会议小结」发言(轮次+参会)。③`看板数据._会议大厅事件` 流程类加 收敛判断/会议小结/插话响应/未谈拢/要问船主 → 开会轮次推进永久显示大厅(刷新也在、带时间戳)。
  - **自检**：py_compile(办公室/圆桌/看板/会议)✓、协同 API 齐✓、看板.大厅能跑(事件 34→37，多出的正是之前隐藏的开会过程记录)✓。**前端不用改/不用 build**(都走大厅已有的气泡/里程碑渲染)。真效果待船主**重启后端**验：点打回看大厅有记录、开会看进度留痕。
- 2026-06-23（接任舟·船主测后修 3 件 + 终止补到位）：
  - **#2 停止停不了开会** → 修好：`升级_执行` 加 `_会在跑` 计数 + `开会开始/结束`(适配.发起任务包它),`停止()` 把开会算进空闲判断(不再"没在跑");`升级_圆桌` 引入 `引擎工具 as 巧`,发言间看急停旗→收摊(返回 `["__停止__"]`,分解/kickoff 干净结束、不留待续残记录)。
  - **终止要"瞬时"(船主点出我没改到位)** → `_问agent流式` 照搬执行层 `监视急停` 模式:后台 task 每 0.4s 看旗→`agent.interrupt()` **当场掐断正在说话的 agent(含思考中)**,只吞 CancelledError、真发言失败仍抛给调用方照报。配合发言间 bail = 真瞬时终止。(原先只"发言间"看旗=要等这句说完才停,错。)
  - **#3 进度卡跑你说话上面** → 改:卡渲染到 `临时` 里最后一条"新任务"分割线下方(不再飘到 feed 顶),sticky 定住;完成 `end` 冻结秒表;`发言开始` 事件带 `轮`,卡显示"第N轮·谁在发言"。
  - **#1 连接中断** → 真因未拿到(旧 backend.log 是 6-18 废日志):`打开办公室.command` 改 `python -u … 2>&1 | tee 运行状态/as_log/backend.时间戳.log`(实时留底),`/stream` except 打出断开真因。glm 慢按船主令不动它(思考深=智力,看不见等于没有)。
  - 自检:py_compile(7文件)+bash -n+tsc 0错+vite build(39模块)全过。**待船主重启端到端验,验"好"再提交本批(还原点 eaae9c0)。**
  - ✅ **已全部落盘 + 自检过（2026-06-23）**：改动文件=`协同.py`/`会议.py`(bug2 `_列表化`)、`升级_模型层.py`(deepseek context_size=1M)、`升级_圆桌.py`(bug1乙:持久agent+增量喂法、bug6发言提示砍车轱辘、bug5汇总结论要实质、发言开始带轮次、docstring纠顺序非并行)、`升级_执行.py`+`升级_办公室适配.py`+`办公室.py`(急停瞬时停止键:批次锁/停止()/启动清残留旗/`/panic`改瞬时)、前端`hallStore.ts`(RunCard加end/当前/轮)+`HallView.tsx`(bug4跟随Ref不被拽回、bug3进度卡固定顶部+冻结秒表+显示轮次/谁在说)+`actions.ts`+`Rail.tsx`(急停按钮改单纯"停止")。自检:py_compile 8文件✓、tsc 0错✓、vite build 39模块✓、模块真import无名字错误✓。bug7(插话重启)船主已自测修好、不动。**未烧钱真跑——待船主重启后端(吃新圆桌/执行/模型层)+浏览器硬刷新(吃新dist)端到端验。**
