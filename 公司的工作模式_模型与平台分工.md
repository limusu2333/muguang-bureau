# 公司的工作模式 · 模型与平台分工

> 决策人：船主（示例主人）。记录：舟（2026-06-26）。
> **改任何跟"模型怎么工作""前端显示什么"有关的代码前，先完整读这份。**
> 来历：船主花了大量时间纠正舟反复理解偏掉的这件事——这份文档是为了让下一个舟不再从头走这条弯路。

---

## 一、最重要的一条原则

**平台不规定人怎么工作。**

《公司的真谛》说：怎么做，交给人；能不能，留给平台。

这条原则落到"模型如何处理任务"上，意思是：

- **模型（人）自己决定**：要不要先列清单、要不要并行派出多个子agent、要不要一波流直接做——这是它的能力，是它的工作风格，不是平台该管的事
- **平台负责**：提供工具（能用什么）、权限（能不能）、可见性（船主看得见）、干预能力（船主能随时介入）

平台不能用代码把模型的工作方式写死。任何"必须先开会"、"必须先列清单"、"必须串行执行"的硬编码，都是在阉割模型的原生能力。

---

## 二、不同模型的工作方式不同（这是它们的智能，不是 bug）

每个岗位背后的模型有不同的工作习惯：

| 岗位 | 模型 | 典型工作方式 |
|---|---|---|
| 老钟（项目经理） | gpt-5.6-sol | 擅长规划、复杂判断与工具协作；可能先列任务清单再派发，也可能直接处理简单任务 |
| 老梁（首席工程师） | qwen3.8-max | 复杂工程任务，自己读文件查代码，逐步推进 |
| 阿强（工程师） | glm-5.2 | 工程落地，工具调用准确 |
| 老纪（测试工程师） | deepseek-v4-pro-0813 | **原生支持并行 tool call**；给它查 bug，它可能同时发出"读文件A"+"读文件B"+"搜关键词C"三个工具调用并行跑，收回结果再综合判断 |
| 阿言（文案工程师） | deepseek-v4-pro-0813 | 同上 |

**关键**：同一个任务，老纪可能一次发出 5 个并行调用；老钟可能先写出一段计划文字再调用工具。这不是我们能预测或控制的——也不应该去控制。

---

## 三、并行执行：框架已经支持，不需要我们做什么

AgentScope 的 `Agent._batch_tool_calls` 会自动判断：

- `FunctionTool(fn, is_concurrency_safe=True)`（**默认值**）→ 多个工具调用打包成 "concurrent" 批，用 `asyncio.gather` 并发跑
- `is_concurrency_safe=False` → 顺序执行

我们所有的平台工具（`hold_meeting`、`assign_task`、`find_colleague`、`Read`、`Grep` 等）都是默认 `is_concurrency_safe=True`。

**所以**：当 deepseek-v4-pro-0813 决定在一次 response 里发出 3 个 tool call，AgentScope 会自动并发跑这 3 个。我们不需要额外配置。

**不要**加任何强制串行的逻辑。不要把工具改成 `is_concurrency_safe=False` 除非有明确的冲突理由（例如写同一个文件）。

---

## 四、可见性：平台的职责

模型并行工作、自主决策——但船主要看得见。《公司的真谛》第五节：自主 ≠ 黑箱。

### 事件流设计（`活_本人.唤醒本人`）

每个工具调用有完整的生命周期事件：

```
子任务开始  {类型: "子任务开始", 发言人: "项目经理", 任务id: "abc12345", 工具: "hold_meeting"}
  ↓（工具内部运行，可能调用嵌套 agent）
  嵌套发言结束  {类型: "嵌套发言结束", 发言人: "首席工程师", 内容: "...", 嵌套: true}
  嵌套发言结束  {类型: "嵌套发言结束", 发言人: "工程师", 内容: "...", 嵌套: true}
子任务结束  {类型: "子任务结束", 发言人: "项目经理", 任务id: "abc12345"}
```

如果模型并行发出多个 tool call，会同时出现多个「子任务开始」，同时处于运行状态。前端工作看板会同时显示多张运行中的卡片。

### 嵌套可见性（`_progress_var` contextvar）

`hold_meeting`、`find_colleague`、`assign_task` 内部调用 `唤醒本人` 时，通过 `contextvars.ContextVar` 把外层 progress 回调传进去。内部每人的发言最终以 `嵌套发言结束` 事件冒泡到外层，显示在 PM 气泡的折叠区和工作看板的子任务详情里。

asyncio 任务继承 context，所以并发工具调用各自拥有独立的 contextvar 副本，不会串扰。

---

## 五、前端：工作看板（不是"模型在思考"）

**旧的心智模型（错的）**：大厅里展示一个模型的内心独白——思考链、工具调用名称、输出。

**正确的心智模型**：大厅展示公司在工作——哪些部门在同时运转、每个部门在做什么、点进去看详情。

### 工作看板（RunCardView → WorkBoard）

公司工作时，进度卡变成工作看板：

```
公司工作中 · 2 项并行          12.3s
┌──────────┐  ┌──────────────┐
│ 🗣 开会  ⚡ │  │ 📋 派活  ⚡  │
│ ▸ 3 条  │  │              │
└──────────┘  └──────────────┘
```

- 每个子任务一张卡，⚡ 脉冲表示运行中
- 点开卡片看内部发言（会议里每人说了什么、派活任务的汇报）
- 多个并行任务同时显示——模型怎么工作，界面就怎么显示

### 工具名到中文标签的映射

`hold_meeting` → 开会，`assign_task` → 派活，`find_colleague` → 找人，`Read` → 读文件，`Grep` → 精确搜文件，`unified_search` → 统一搜证据，`recent_hall_chats` → 按时间翻近期，`report_to_owner` → 汇报，`ask_owner` → 请示。

`semantic_search` 和 `search_hall_history` 只保留底层兼容，不再装进现役工具箱。`unified_search` 同时查公司文档、大厅原话、当前本人私册和公共图谱，并返回出处与逐源故障；`recent_hall_chats` 不按相关性排序，只用于“最近几天聊了什么”。其他工具名直接显示原名。

---

## 六、干预（掌舵权）：船主随时能介入

《会议的真谛》：插话 = 掌舵权，全员立刻听到立刻调整。

### 当前实现（v1）

在 `唤醒本人` 的 `reply_stream` 循环里，每收到一个 `ToolCallEndEvent`（子任务完成），检查事件总线里有没有船主的插话。如果有，注入进 agent 的 context，让模型在下一步推理时能看到。

```python
elif isinstance(ev, ToolCallEndEvent):
    # 检查插话队列 → 注入 agent.context
```

**触发时机**：子任务与子任务之间（工具执行完毕、模型还没开始下一步推理之前）。

**前端触发**：大厅输入框在「公司工作中」状态下，发送按钮变为「插话」，走 `/插话` 端点进事件总线。

### 局限（v1 的边界）

- 只能在工具调用的间隙注入，不能打断正在流式输出的模型推理
- 并行工具同时跑时，注入时机是第一个完成的工具结束后
- v2（真正的 mid-stream interrupt）需要 AgentScope 框架级的 interrupt 机制，待后续

---

## 七、不该做的事

1. **不要强制串行**：不要把工具改成 `is_concurrency_safe=False`，除非有真实的写冲突
2. **不要在平台层规定模型的工作流程**：不要写"先开会再派活"这类顺序逻辑——那是阉割
3. **不要把"思考链"当一级展示**：思考链是调试信息，折叠在气泡里；工作现场才是一级视图
4. **不要预设模型会列清单**：有的模型会，有的不会，这是它们各自的智能，前端应该都能处理
5. **不要忽视"模型是人，公司是平台"这条根**：每次想改执行逻辑时，先问：这是在规定怎么做（不该管），还是在设置能不能（该管）？

---

## 八、相关代码位置

| 功能 | 文件 | 关键函数/变量 |
|---|---|---|
| 子任务生命周期事件 | `工具/活_本人.py` | `唤醒本人`，`_tasks` dict，`ToolCallStartEvent`/`ToolCallEndEvent` |
| 嵌套可见性 | `工具/活_本人.py` + `工具/平台_工具集.py` | `_progress_var` contextvar，`_嵌套progress工厂` |
| 并行执行 | AgentScope 框架内部 | `Agent._batch_tool_calls`，`is_concurrency_safe` |
| 工作看板 | `前端/src/components/HallView.tsx` | `RunCardView`，`SubtaskCard` |
| 子任务状态追踪 | `前端/src/hallStore.ts` | `子任务列表` state，`子任务开始`/`子任务结束` 事件处理 |
| 干预注入 | `工具/活_本人.py` | `ToolCallEndEvent` 处的事件总线检查 |
| 模型工作特性 | 花名册.yaml | 每个岗位的模型名，能力描述 |

---

## 九、给下一个舟的话

船主在 2026-06-26 花了大量时间解释这件事。核心是两点，记死：

1. **DeepSeek V4-Pro 等模型原生支持并行 tool call（最多 128 个）**，它们在处理任务时可能同时派出多个子 agent——这是它们本来就有的能力，不是我们实现的。平台的工作是让这个能力正常工作，而不是把它变成串行。

2. **前端的心智模型要是"公司在工作"，不是"模型在思考"**。你看到的应该是：有几个任务在并行跑、每个任务是什么、点进去看详情——像看一个公司的工作现场，不是看一个模型的内心独白。

这两条理解对了，这个系统的所有设计决策都能推出来。

*本文是"模型与平台分工"这个议题的对齐链根。改相关代码前先读这份，不要自己推测。*
