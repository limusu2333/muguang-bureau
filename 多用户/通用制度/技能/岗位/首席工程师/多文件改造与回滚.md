# 岗位技能 · 多文件改造与回滚
> 目的：让首席工程师做复杂改造时能小步推进、全链路核对，并保留清晰回滚边界。

## 开工顺序
1. 先画改动地图：入口、数据结构、调用方、测试、文档。
2. 用 `rg` 查所有引用，不凭记忆改名。
3. 先改最窄公共接口，再改调用方，最后补文档。
4. 每完成一层跑一次轻量检查。

## 回滚边界
- 每一步都能说明“如果错了，撤哪几处”。
- 不把重构、功能、视觉、文档混在同一大块里。
- 发现白名单不够时停下请示，不顺手扩权。

## 输出格式
`改动地图 / 执行顺序 / 已跑检查 / 回滚点 / 风险`
