# B · 公司装修方案 · 总蓝图（死平台是场所+规则，活人是本人）

> **历史蓝图，不是现役运行说明（2026-07-14 标记）**：当前房间、部门、搜索和记忆路径以 `README.md`、`班规.md`、`设计/记忆系统_全图.md` 及代码实测为准。旧搜索工具名只用于追溯；现役统一入口为 `unified_search`。

> 2026-06-25 重画（地基夯实后第二版）。**唯一上位依据**：`公司的真谛_人是使用者平台是死物.md`（架构根，已含全员活人/人岗解耦/记忆跟人/场所不是工具/来的是本人/可持续红利）+ `会议的真谛_…md`（开会根）+ `调研_公司架构_业界现状_byCodex.md`（业界佐证）。
> 本文是**落地总蓝图**，待船主逐阶段拍板；每阶段可验可回滚。
> 船主对本方案的圣域要求：**高度俯视、细节把控、效果达标——不是修一个功能就忽略其他和高度。**
>
> **第一性判据（每改一行先问）**：这是「场所+规则」（死物，留）？还是「替活人判断/编排怎么做」（删，还权给本人）？还是「能不能/资格」（权限，焊平台）？

---

## 一、目标全貌（俯视）

### 死平台层 = 场所 + 规则（客观存在的死物，不是谁的工具）

**每个房间 = 一个真实存在的场所 + 一块「规则牌」（使用说明书：这里干嘛、规矩、能做什么）。人本人自己进，读规则牌就知道怎么用。**

| 房间（场所） | 规则牌（写明、上墙、谁来都能读） |
|---|---|
| **大厅** | 老板在这发话；公司的人在这看到、响应；对话永久留存、按天归档 |
| **会议室** | 谁想讨论就**发起一场会**、相关的人被通知**自己决定来**；发言有秩序（轮流真听=会议真谛）；老板可随时插话掌舵；收敛靠共识/主席判够 |
| **工作区** | 领了活在这做；写文件走权限白名单；产出要经验收 |
| **资料室** | 缺证据来这查（搜索/语义检索/读文件）；**不许编造** |
| **记忆库** | 每个**人**的一生记忆（按人存、跟人走）；只有明确"教导"才写入；防污染 |
| **公告栏** | 岗位职责册上墙：每个岗位干嘛、汇报线、能拍什么板、权限边界 |
| **请示台** | 删除/花钱/密钥/最终交付/越界 → 来这请示老板，等老板拍板 |

**工位 + 岗位（死椅子）**：项目经理/首席/工程师/测试/文案，每个 = 职责册 + 岗位视角 + 权限模板 + 工位。椅子不动、不记事。

**工具（平台供、本人取用）**：查资料 / 发起会议 / 派活 / 验收 / 找人协调 / 请示 / 报告。

**线路 = 事件唤醒机制（死平台的神经）**：老板发话、有人发起会议、有人喊你——都是**事件**，平台广播。事件触发时**唤醒"本人"**（加载那个人的连续身份+记忆），本人来响应——**不是 new 一个空壳执行一步**。

### 活人层 = 本人（在规则内思考、主动求索）

- **人（独立于岗位的实体）**：老钟/老梁/阿强/老纪/阿言，各有名字 + **一生连续记忆** + 工作习惯（一波流/列清单/派子 agent 是脾性）+ 成长。
- **任命/调岗**：人 ↔ 岗位映射；**调岗带记忆走**（带着过往的老手，不是清空的新人）。
- **被唤醒的是本人**：事件来 → 唤醒本人（带它的记忆/身份）→ 本人到所在房间/岗位 → **读规则牌、知道目的** → 在规则内**主动求索**（自己想、自己查、自己判断要不要找人），不是被动等指令回一句。
- **横向协调**：人之间彼此可见、可呼叫（「找人协调」）——工程师问首席、找测试，**不是项目经理单点遥控**。

### 四道闸（焊平台，绝不交给 agent）
权限 / 可见性（全程进事件流→窗户）/ 预算（步数·token·开会轮数上限）/ 评估（任务集，不靠"看着会开会"验收）。

### 可持续红利（这套架构为什么值得做）
**以后加功能 = 建一个房间 + 写好规则牌，活人读规则就会用**——不改 agent、不改流程、不动别的房间。开发重心从"写死流程"挪到"把规则写清楚"（=管真人公司：写明手册、招对人、他们自己会用）。

---

## 二、现状账（A 阶段·拿那把尺过后端真代码）

| 部件 | 现在干什么 | 尺判 | 判决 |
|---|---|---|---|
| `升级_圆桌.kickoff/分解` | 写死立项流水线（议题→必开会→收敛→汇总→工单） | 替 agent 编排"怎么做" | **拆**·还权给本人自主判断 |
| `升级_圆桌._会前议题/_评估收敛` | 代码驱动 PM 出议题/判够没 | 替 agent 判断 | **拆**·还权 |
| `升级_圆桌._圆桌讨论` | 圆桌（真听/收敛/插话） | 一间**会议室场所**（不是工具） | **留**·做成会议室房间，人本人自己进 |
| `办公室.py /判断` | 替 PM 判"聊还是开会" | 替 agent 判断 | **拆**（本人自己判） |
| `办公室.py` 路由/SSE | HTTP/事件流 | 平台线路 | 留·改造成事件唤醒 |
| `升级_执行.py` | 派活/白名单/验收/复盘 | 工作区能力 + 白名单权限 | **留**·做成工作区+派活工具；白名单=权限闸 |
| `升级_权限层` | allow/deny/ask | 权限护栏 | **留+加固**（四道闸·权限） |
| `事件总线` | session 事件流 | 可见性平台 + 唤醒神经 | **留+加固**（可见闸+事件唤醒） |
| `记忆向量`+`岗位/*/记忆.md` | 记忆检索，**按岗位存** | 记忆库平台，绑岗位错 | 机制留·**记忆改绑人** |
| `搜索工具/语义检索` | 查资料 | 资料室能力 | **留**·做成资料室工具 |
| `会议.py` | 工单/会议文件落盘 | 工单状态（平台）+ 流程编排 | 状态留·**拆流程编排** |
| `审批.py` | 请示→PM预审→船主裁决 | 请示台(平台)+船主裁决(权限)+PM固定预审(编排) | 请示台留·船主裁决留·**拆 PM 固定预审** |
| `花名册.yaml` | 岗位↔模型 | 平台配置，**缺"人"维度** | 留·**加"人"层**（人↔岗位任命，人岗解耦） |
| `岗位/*/说明书.md` | 职责册 | 公告栏内容（死椅子规则） | **留**·上墙 |
| `岗位/*/宪法.md` | 岗位视角 | 椅子属性 | **留**·并进职责册 |
| `技能/通用+岗位` | 工作方法/工具用法 | 平台技能（本人取用） | **留** |
| `引擎.py(828)`等旧件 | 自研引擎版残留 | 已被升级版替代 | ⏳ 确认现役范围→废弃归档 |

**一句账**：**拆**「替 agent 编排判断」（立项流水线/_判断/PM固定预审/代码收敛汇总）；**留+加固**平台能力（圆桌→会议室、执行→工作区、搜索→资料室、记忆机制、事件、权限）；**改**「记忆绑岗位→绑人」「花名册加人层」「会议室从工具→场所」「agent从临时调用→本人唤醒」。

---

## 三、改造路径（分阶段·每阶段独立可验可回滚·旧件留到验通才删）

- **阶段 A · 现状账**（第二节，已出）→ 船主认账。
- **阶段 B · 建死平台（场所+规则牌）**：①整理岗位职责册上「公告栏」②把现有能力做成各房间（会议室/工作区/资料室/记忆库/请示台）+ 每间写「规则牌」③权限模板按岗位配好。**先不改控制流，单独验每个房间能进能用。**
- **阶段 C · 招活人（本人 + 人岗解耦）**：①花名册加「人」层（人↔岗位任命；记忆改按人存、跟人走）②岗位 agent 变**被事件唤醒的连续本人**（加载它的记忆/身份，读规则牌、知目的、主动求索）③废 `/判断` 死开关，老板的话进大厅、本人自己判断响应。
- **阶段 D · 横向协调**：人之间能彼此找彼此问（「找人协调」）——不再 PM 单点遥控；会议室=人自己发起/加入的场所。
- **阶段 E · 四道闸 + 评估**：权限/可见/预算全接；建任务集按指标验。
- **前端顺势**（穿插 C-E）：窗户显示全员怎么想、怎么判断、怎么协调（思考流/工具调用已铺垫）。前端那批未完 bug（打回/思考流/说人话）并入此处；「说人话」写进职责册和人的工作守则。

---

## 四、风险红线（codex 划 + 舟守，违反=改造翻车）

1. **不裸奔**：拆流程 ≠ 扔工作标准。职责册/规则牌/红线/四道闸一个不少。
2. **不堆 agent**：先把现有几个人做活、横向协调跑顺；新增岗位按需且能测出收益才招。
3. **没现成成熟答案**：「全员自主经营公司」业界尚无公认范式——在前沿，每步留证据、靠评估验。
4. **成本/失控/评估是最大坑**：预算闸和评估集是命门，不是可选项。
5. **规则牌是手艺**："建房子+写规则人自然会用"的前提是规则写清+模型够强；规则含糊红利打折——写清规则要当核心活干。

---

## 五、要船主拍的（逐阶段）

- **现状账（第二节）认不认**：拆/留/改 三栏有没有漏判错判。
- **起步阶段**：建议从 **B·建死平台**起步——最小风险、单独可验：先把房间和规则牌装好，再招本人住进去（C）。不一次推平。

---

---

## 动工进度

- **2026-06-25 · 第一锤（最小活细胞）✓ 架构验证成功**：
  - 建 `工具/平台_工具集.py`（死平台工具，FunctionTool：Read/Grep/净Glob/semantic_search/report_to_owner/ask_owner，每个带规则说明）+ `工具/活_本人.py`（唤醒本人：读职责牌[说明书+视角]+记忆 → ReAct agent + 工具 + 权限 EXPLORE → 自己判断）。**新旧并存，旧公司一行没碰。**
  - **真跑验证（deepseek 测试工程师本人，62s 跑通）**：本人真的**自己判断**（分析"底层框架"问题）、**主动求索**（去翻文件、列证据链）、**说人话**（"好吧，我现在环境受限…让我直接根据线索分析"——口语、不工程腔堆1234）、结尾还主动说"我权限受限、给我绝对路径我再翻代码看"。**最小活细胞跑通 = 整个改造方向可行、能跑。**
  - **暴露三个待修（都不是架构问题）**：
    1. ⚠️**gpt-5.5 中转站（http 明文 192.0.2.10）在「带工具的 ReAct」下卡死**——干等 8 分钟、CPU 0、sleeping（之前点名只是简单 reply 没暴露）。**PM 是 leader、最需要 ReAct，却卡在这个中转站。需换稳定接入或换模型——待船主（key/接入是船主地界）。**
    2. 本人查不到公司代码真现状（不知公司根绝对路径/工具默认目录不对）→ 凭旧文件名推断、判断错（说"自研框架"，实际是 AgentScope/React）。修：工具默认锚定公司根、把根路径写进本人的规则。
    3. 缺证据时下结论偏肯定（虽结尾补了"受限"）→ 系统提示要强化"查不到就明说没查到、别下肯定结论"（这条对小酒同样命门）。
  - 还原点 = 最后提交（`11b4ccf`）；新文件 `平台_工具集.py`/`活_本人.py` 未提交。
- **2026-06-25 · 第二锤（横向协调）✓**：
  - `平台_工具集.py` 加 `find_colleague`（本人自己找别岗位的本人问/帮忙，复用 `唤醒本人`；被找来的同事 `可找人=False`、只查资料/回答、不再转手找人=防无限转包）；`活_本人.唤醒本人` 加 `可找人` 参数。
  - **真跑验证**：测试工程师本人收到"统一验收报告用词风格"的活 → **自己判断要找文案对齐** → `find_colleague('文案工程师',…)` 叫来文案本人 → 文案给出"说人话"三层标准（原则/红线/落地骨架）→ 测试**汇总对齐结论**交船主。**全员皆活人 + 横向协调跑通，不靠 PM 遥控。**
  - PM(gpt-5.5) 修查证后**判断准确**：真去 read package.json/查 import/看 HTTPServer，给出真现状「AgentScope 2.0.x 核心 + 多模型圆桌 + React/Vite/TS + http.server 外壳」（不再凭旧文件名瞎猜）。gpt-5.5 第一锤的卡是船主并发撞的、本身能用。
  - **核心架构两锤全立住**：本人被唤醒→自己判断→主动查证→说人话→横向协调。改造方向完全可行。
- **2026-06-25 · 第三锤（开会·用本人重做会议）✓**：
  - `平台_工具集.py` 加 `hold_meeting` + `_开一场会`：参会的**本人**顺序发言、每人带前面所有人的完整发言（真听）、主席（项目经理本人）判收敛（语义、不写死轮数）、参会是**本人不是临时 agent**。装进主事本人的工具集（`可找人=True` 才带 hold_meeting，被找/参会的同事不带=防嵌套会议）。
  - **真跑验证（工程师+测试，1 轮）**：测试工程师**真接住工程师的话**（"我跟工程师一个立场，他说的『没复现步骤修不了 bug』站得住"）再从测试视角补（证据链/四要素）= **会议真谛的顺序真听生效**，发言主体是本人、说人话、不表演。
- **★ 核心三锤全立住 = 活公司核心协作能力验证完成**：①最小活细胞（本人被唤醒→自己判断→主动查证→说人话）②横向协调（本人找本人）③开会真听（本人们走进会议室、接彼此的话）。**死平台+活人架构落地可行、能跑，不是流程驱动。**
- **剩下（更大阶段·新旧并存先建后切）**：①**派活工具**（嵌套执行引擎，⚠️涉及真写文件/权限，需谨慎）②**大厅接入**（老板发话→唤醒 PM 本人自己处理；改办公室控制流，新入口、不拆旧 /trigger）③**记忆改按人存**（人岗解耦，记忆跟人走）④前端窗户显示本人怎么想/怎么协调。
- **2026-06-25 · 第四锤（派活·真写文件）· 机制通、完整路径待调**：
  - 加 `assign_task`（派活给岗位本人）+ `干活工具集`（Write/Read/Edit + 权限白名单门禁）+ `唤醒本人` 干活模式（`建权限上下文` 按白名单把门）。
  - **关键技术坑填了（记死，给下一个我）**：
    1. **本人执行工具必须用流式 `reply_stream`**——官方 Write/Edit 在非流式 `agent.reply` 下停在「Waiting for tool calls to be confirmed」、**不真执行**；流式下才真写（实测：简化命令+流式，本人真写出文件）。执行引擎 `升级_执行` 自己代码都把「Waiting for tool」当熔断处理、踩过此坑没根治。**已把 `唤醒本人` 全改流式（收 TextBlockDelta）。**
    2. **白名单要同时收相对+绝对 glob**——模型常写相对路径，只给绝对会被官方 Write 的 fnmatch 拦（实测绝对 ALLOW、相对 ASK）；目录要补 `**`。
  - **机制验证可行 ✓**：简化命令 prompt + 流式，测试工程师本人真写出文件。
  - **✓ 真根因找到 + 跑通**：6 次失败的真根因是**白名单 bug**——`str(COMPANY/目录)` 被 `Path` 吃掉尾 `/`，绝对规则变精确路径（无 `**`），本人写的绝对文件路径 fnmatch 匹不上 → 一直 ASK 不执行（"话痨/没说话"全是表象）。修：目录白名单拼接后**统一补 `/**`**。修好后 **deepseek 测试工程师本人真写出文件**✓。顺手把干活 prompt 改精简任务导向（完整身份哲学会让模型话痨，无害优化）。小瑕疵：deepseek 干活只 thinking+tool_call 没 text（返回"没说话"），但文件真写出——后续可让它写完调 report_to_owner。
  - **★ 核心四锤全跑通**：判断 ✓ 横向协调 ✓ 开会真听 ✓ 干活（真写文件+白名单门禁）✓。**活公司的核心协作 + 执行能力全验证能跑。**
- **★ 这一程（2026-06-25）总账**：立两条根（`公司的真谛` 整合船主全部设想 + 本装修方案）；**核心架构验证成功**——活细胞（本人自己判断+查证+说人话）、横向协调（本人找本人）、开会真听（本人走进会议室）三锤真跑通；派活机制可行、完整路径待调。**死平台+活人的味儿，验证能跑。**
- **2026-06-25 · 记忆跟人走（人岗解耦·核心遗漏补上）✓**：记忆从「按岗位存」改成「按**人**存（`记忆库/<人名>.md`）、跟人走」。`活_本人` 加 `_岗位的人`（查花名册名字=岗位现坐的人）、`读其人记忆`（按人名）、`给某人记一笔`（按人写）；唤醒本人按"人"取记忆。验证：给老钟记一笔→`记忆库/老钟.md`，老钟换任何岗位记忆都跟着他走。这是船主「记忆是人的、不是岗位的、把它们当人」的落地。
- **★ 这一程（2026-06-25）总账**：①立两条根（`公司的真谛` 整合船主 11 段圣域全部设想 + 本装修方案）②核心四锤验证：判断 / 横向协调 / 开会真听 / 干活（真写文件+白名单门禁）③记忆跟人走（人岗解耦）④舟的高度全面核对 + 遗漏清单。**死平台+活人的根，从灵魂到核心能力，落地验证能跑。**
- **剩（更大工程·清晰交接）**：大厅接入（老板发话→唤醒 PM 本人自己处理，新入口、不拆旧 /trigger、废 `/判断`）→ 可见性窗户（本人思考/协调进事件流→老板可见，自主≠黑箱）→ 评估（任务集）→ 前端 → 场所vs工具形态。**新东西全是独立新文件，旧公司一行没碰、照常能起（已编译确认）；还原点 `11b4ccf`、全部未提交（让船主回来或下程决定提交，避免半成品架构混提交）。**
- **2026-06-25 · 合并成一个公司 + 文档建设（船主令"只要一个公司"）**：①`README.md` 重写成「一个公司」总览（活公司=方向、文档地图、代码地图、合并路线、班子人名）②文档体系收敛：根（两真谛）+ 蓝图（README/装修方案）+ 佐证（调研/班规/花名册）+ 历史归档；过时 A 系列过程方案（AgentScope原生重建/前端逐条对照/动手升级）归档进 `_历史_勿执行/`。**方向上收敛成「一个公司 = 活公司」。** 代码层面的完整合并（办公室界面切活公司 → 本人过程接事件流前端可见 → 旧流程驱动物理归档）= 推翻级大工程、本程 token 做不完，**合并路线一步步写清在 README**；旧公司过渡期能用、活公司独立可跑，下程按路线执行切换。
- **未提交**：`平台_工具集.py`/`活_本人.py` 新文件 + `活_本人` 系统提示/流式执行。旧公司一行没碰、照常能跑。还原点 `11b4ccf`。

---

## 舟的高度 · 全面核对（2026-06-25，对照《公司的真谛》逐维度，船主令"查遗漏直接改"）

| 根的维度（出处） | 状态 | 遗漏 / 下一步 |
|---|---|---|
| 公司死平台 / agent 活人（③） | ✓ | 四锤验证 |
| 智能长在人身上·内心独白（④） | ✓ 本人自己判断/用工具 | ⚠️ 还没接大厅入口（老板发话还走旧流水线） |
| 全员皆活人（⑥三续1） | ✓ 多岗位本人 | — |
| 横向协调（三续1） | ✓ find_colleague | — |
| 能力 vs 权限（⑤四节） | ✓ 本人自主 + 白名单 | — |
| 废 `/判断` 死开关（二节） | ❌ 旧流水线/判断还在 | 大厅接入时废（新旧并存中） |
| **人岗解耦·人层（三续3）** | ❌ **没做** | 唤醒按岗位，无"人"层 |
| **记忆跟人走（⑦、三续4）** | ❌ **没做** | 记忆还按岗位存——**根的核心遗漏，本程补** |
| 工作习惯/成长（⑦） | ❌ | 人层的一部分 |
| 场所≠工具·会议室是场所（⑨三续2） | ⚠️ 部分 | 开会是 hold_meeting 工具，未到"场所人自己进"形态 |
| 来的是本人·连续身份（⑩） | ⚠️ 部分 | 每次新建 agent+读记忆；记忆跟人做了才算真本人 |
| 可持续红利·规则牌（⑪三续3） | ⚠️ 架构支持 | "规则牌"体系没系统化 |
| 四道闸·权限 | ✓ 白名单 | — |
| **四道闸·可见性（五节·窗户）** | ❌ **没做** | 本人思考/协调没接事件流→老板看不到（违背自主≠黑箱） |
| 四道闸·预算 | ✓ 预算步数 | ⚠️ token/费用上限可加强 |
| 四道闸·评估 | ❌ 没做 | 待做 |
| 大厅接入·端到端 | ❌ 没做 | 活公司没接老板入口 |
| 前端窗户 | ❌ 没做 | 待做 |

**遗漏按重要性排序**：①**记忆跟人走+人层**（根的灵魂、"来的是本人"的前提）②大厅接入（端到端：老板→本人）③可见性窗户（自主≠黑箱）④评估 ⑤前端 ⑥场所vs工具形态。
**本程直接补**：①记忆跟人走（最核心、代码可控）。②③④⑤⑥ 是更大工程，清晰交接。

---

## 2026-06-25 · 合并动工：活公司接到办公室入口（端到端跑通）

船主令"彻底弄完再让我试、只要一个公司"。把活公司接到办公室界面，三步全做完 + 端到端验证：

1. **本人过程可见（自主≠黑箱）**：`活_本人.唤醒本人` 加 `progress` 参数——把本人的「发言开始/思考增量(ThinkingBlockDelta)/工具动态(ToolCallStart)/发言增量(正文)/发言结束」全吐给 progress → 事件总线 → 前端窗户。老板看得见本人怎么想、查什么、调什么。
2. **办公室入口接活公司**：`办公室.py` 加 `_活厅` 方法 + `do_POST` 路由 `/活厅`——老板发话 → 后台线程 `asyncio.run(唤醒本人("项目经理", text, progress→事件总线))`，立即返回 sid，前端 `/stream` 看流（复用 `_trigger` 的后台机制）。**这替代旧的 `/判断`+`/trigger` 流水线。**
3. **前端大厅走活公司**：`hallStore.智能发送` 去掉 `/判断` 死开关、直接 `立项`；`立项` 入口 `/trigger`→`/活厅`；完成处理认 `result.报告`（本人报告已通过发言气泡显示，不重复系统消息）。tsc 0 错 + build✓。

**✅ 端到端验证跑通**：`唤醒本人("项目经理", "公司用什么框架?简单回我", progress=收集)` → **13s**，过程事件 `{发言开始:1, 工具动态:2, 发言增量:36, 发言结束:1}`，PM 本人**自己用了 2 次工具查证**、报告「前端 Vite+React+TS，后端 AgentScope ReAct」——**老板发话→PM本人自己判断+查证+报告、全过程可见，端到端通了。**

**⚠️ 还差最后一步才算"合并完成"（替身接着做）**：
- **前端真机验**：船主重启 `打开办公室.command` + 硬刷新，在大厅真发一句话，确认走 `/活厅`、看到 PM 本人思考流/工具动态/报告。（我只在 python 层验了 `唤醒本人`+progress，**HTTP 端到端[办公室起→浏览器发→/活厅→/stream显示]还没真机验**——这是替身第一件事。）
- **旧入口/代码物理归档**：`/判断`、`/trigger`、`升级_圆桌`(流水线)、`升级_执行`、`升级_办公室适配`、`会议`、`审批`、`协同`、`引擎`、`页面` → 验前端真机通过后，移进 `_历史_勿执行/`。（现在新旧入口并存：`/活厅`是活公司、`/trigger`旧流水线还在但前端不再调；保留到真机验通再删。）
- 嵌套过程可见性：PM 本人调"开会/派活"工具时，工具内部的本人（`hold_meeting`/`assign_task` 里 `唤醒本人` 没传 progress）过程暂不可见，只见 PM 看到的工具结果。下一步可给嵌套工具透传 progress。

---

## 2026-06-26 · HTTP 端到端真机验 + 场所对话持久层（接任舟，浏览器真验）

**先做了交接点名的"前端真机验"，逮出并治了一串真 bug——`合并完成` 远不止真机验+归档。**

**① HTTP 全链路真验通**（curl + 真浏览器 preview）：`POST /活厅 → /stream SSE → PM 本人查证+报告`，浏览器真发一句也通（用 React fiber props 直调 send 绕过自动化触发障碍）。暗房亮桌三栏、真模式、班子 6 人都在。

**② 逮到真 bug：活厅路径对话发完就消失**（真机验该逮的）。根因（代码坐实）：前端 `清乐观()` 把"话"气泡当乐观气泡清掉、指望从永久 `/hall` 拉回；旧 `/trigger` 写 会议/协同 文件、`看板数据.大厅()` 读得到，**活厅只往事件总线吐 SSE、没落盘任何永久层** → 清掉就真没了。违背大厅规则牌"对话永久留存"。

**③ 修法（船主拍板走 B + 圣域⑫）：给「场所」自己的对话持久层（场所内生功能、忠实记录、按天归档）**：
- `工具/大厅记录.py`（大厅的痕迹簿）：`_活厅` 把船主发话 + PM 本人报告忠实落 `大厅/对话/YYYY-MM-DD.jsonl`。
- `工具/会议室记录.py`（会议室的痕迹簿）：留痕写进会议室场所逻辑 `_开一场会`、**逐条即时**（开场记一笔、每句发言当场记一句，中途散场也留痕；不是 hold_meeting 收尾补记）。落 `会议室/对话/YYYY-MM-DD.jsonl`。
- `看板数据.大厅()` 把两个场所的对话作正源读、按二级精度时间排好并进时间线（k:话 气泡 + k:会 里程碑），前端零改动。
- **验**：浏览器真发 → 落盘 → 刷新全存活；真发会议需求 → PM 召集 3 人开 2 轮 6 发言真会 → 会议室逐条留痕 → 大厅可见 → 刷新存活。

**④ 路上修的两个真阻断（都查了真因）**：
- **参会名匹配太脆**：PM 写的岗位名差一点就静默不开会、自说自话什么都没记 → `平台_工具集.归一岗位` 容错（测试/首席/文案/工程→正式岗位名），find_colleague/hold_meeting/assign_task 同治。
- **权限焊错地方**：活厅 PM 跑 `EXPLORE`（纯只读），agentscope 引擎只放行只读工具 → **hold_meeting/find_colleague/assign_task 全 DENY，PM 能想不能动、开不成会**。按"能力 vs 权限"修：`活_本人` 协作模式 EXPLORE→DEFAULT + 显式放行协调动作（写文件红线仍在干活白名单把关、删除花钱仍 ask_owner）。

**⑤ 圣域升格**：船主把"忠实记录"点透为**场所的内生功能**（⑫）——已钉进《公司的真谛》原话区。

**护栏锤（归档前命门）· 船主拍板走「丙·按红线分流」**：简单安全的（白名单内、非交付）直接做并留痕给船主看；碰**最终交付/花钱/删除/越界**才停下等船主拍。
- **① 红线先批 ✓已做并验**：`ask_owner` 从"写孤立文件（船主看不见=黑洞）"改成走 `审批.创建请示(直接上报=True)` → 进右栏「等你裁决」、船主能裁决（PM 本人自判要请示=已是上报级，不再经理预审）。验：调一次 → 审批待船主 + 看板待裁决都收到。
- **② 最终交付后验 ✓已做并验（2026-06-26）**：新增 `工具/待验收记录.py`（活公司产出持久层，仿大厅/会议室记录）；`assign_task` 完工后调 `待验收记录.新增()` 标待验收；`看板数据.待办汇总()` 加 `活公司待验收` 字段；`办公室.py` 加 `/活厅_验收` 端点；前端 Action.tsx 渲染带「批准/打回」按钮。真机验通：记录写入→右栏显示→批准→消失→大厅留痕。
- **③ 裁决留痕 ✓v1已做（2026-06-26）**：批准/打回调 `大厅记录.记一句()` 留永久痕迹；PM 不自动 resume（v1）——要续靠船主再发一句重新唤醒。v2（事件驱动异步 resume）是硬骨头，待后续。
- **归档旧件**：② 已落地，船主现在能看见并拍最终交付 → **可以归档旧流水线了**（升级_圆桌/升级_执行/升级_办公室适配/会议/审批/协同/引擎/页面）。归档前确认旧 /trigger 前端已不调用。
- 嵌套过程可见性（hold_meeting/assign_task 内部本人过程没透传 progress 给窗户）仍未做。
- **已提交**（分支 `agentscope-migration`）：`26f3436`=场所持久层+协调权限+HTTP验；`523dfbc`=护栏丙①；`f9662e4`=护栏丙②③v1（待验收记录+验收闸+裁决留痕）。还原点 `11b4ccf`。运行时对话痕迹/待验收 jsonl 不入仓。

## 2026-06-30 · 彻查收口：旧按钮只读、真实身份、拒绝假成功、规则牌归位

船主令“彻底审查整个公司”后发现四类会破坏活公司根的残余：

1. **旧流程按钮还在前端显得可执行**：右栏和项目室仍展示旧工单的确认开工、续步、停止、追加、通过/打回/作废按钮，但后端旧路由已经 stub 或属于归档流程。已改：前端不再调用 `/confirm`、`/resume`、`/stop`、`/append`、`/verdict`、`/reject_decomp`；旧工单统一显示为“历史入口/旧工单”，只保留打开项目室查看。活公司请示与活公司验收仍可拍板。
2. **请示身份硬写项目经理**：`ask_owner` 以前无论谁请示都落款“项目经理”，不符合“来的必须是本人”。已改：从 `活_本人._当前岗位_var` 取当前真实岗位；如果没有真实岗位上下文，直接拒绝代请示。
3. **交付/验收有假成功风险**：`assign_task` 以前待验收登记失败会吞异常、仍说“已进待你验收”；`待验收记录.批准/打回` 找不到记录或大厅留痕失败也可能被端点返回 ok。已改：登记失败明确回“不能算已交付”；批准/打回必须真改到记录并写入大厅留痕，否则前端显示验收失败。
4. **员工规则牌还残留旧训练**：班规、岗位说明和技能包里仍把“工单/分解/待确认”当默认语言，会把员工带回旧执行器。已改：项目经理技能改成“先判断、需则派活”；工程师/测试/文案技能改成“当前活/历史工单兼容”；`办公室.py` 注入上下文不再要求默认读工单和输出 JSON；`工单模板.md` 标注为历史模板。

这次收口不是改几个 bug，而是把《公司的真谛》的尺重新焊回代码和规则牌：**人必须是真的、场地必须留真痕、按钮必须对应真实能力，员工不能被旧训练牵回流程零件，不允许页面和回复制造“已经发生”的幻觉。**

## 2026-07-02 · 舟的彻底审视 + 按船主令逐条修缮（Fable 5 回归第一程）

船主令"彻底审视公司：找问题、看方向、寻隐患，公司到了分岔路"。舟通读全部代码/文档/数据/事件日志后交账，船主拍板"一条一条改"（GPT 中转站 http 一条豁免不改）。

**审计总判断**：架构骨架立住、真谛贯彻良好（能力vs权限的尺、不保底、假发生已封）；但公司建成至今没干过一件真活（大厅两天五句话、工单全空、记忆库全空）——分岔路的答案 = 停止无限打磨，收资产、补接活必翻车的缺口、接第一件真活（且该为她干）。

**本程已修（全部落库）**：
1. 资产落袋：06-30 彻查改动 + 会议室.py（此前 untracked 零回滚网）全量提交；`备份公司记忆.command` 一键快照活记忆到 `../90_备份/`（真备份仍需船主拷移动硬盘=悬置④）。
2. 干活的人是本人（圣域⑩）：干活模式带名字+记忆（任务导向语气保留，防话痨的教训不丢）。
3. 成长闭环（圣域⑦）：assign_task 完工→平台给干活人记一笔事实（任务+动了哪些文件）；船主验收批准/打回→再记一笔（含批注）。记忆库从此开始积累。
4. 走廊留痕（圣域⑫）：find_colleague/assign_task 的往来落 `走廊/对话/*.jsonl`（新建 走廊记录.py；不摊进大厅防噪声，痕迹永在）。
5. report_to_owner 落大厅痕迹簿——06-30 那份被停止吞掉的评测报告不会再发生（那份报告已从事件日志捞回给船主看过）。
6. **执行室**（真谛⑪"加能力=建房间+写规则牌"；船主定性：不是测试员不行，是工具间没建好）：`执行室.py` run_command——允许名单（python3/pytest/node/tsc/make/git只读等）、不开shell、目录锁公司/产品仓、超时+截断、每次执行落 `执行室/记录/*.jsonl`。装进干活工具集。自检过：放行测试/只读，拒绝 git push/rm。
7. 验收看得见产出：唤醒本人 从工具轨迹收集真实写/改的文件路径 → 待验收记录带 `文件` 字段 → 右栏验收卡显示"真实动了的文件"（没动文件也明说）。
8. 插话去重：只有根单元和会议室订阅插话；嵌套本人不再各自消费（治一句插话被 N 处同时吃）。
9. 费用闸 v1：每会话动作数硬上限 200，到顶暂停等船主（治"很多人各自合法跑很多步"的总量失控）。
10. 大厅附件真接通：＋按钮/粘贴截图→base64→后端保存；图片诚实标注"班子看不到画面内容"（禁装看过）。工作中插话不带附件（防静默丢弃=假发生）。
11. 读网页防注入标注：外部网页内容≠指令。
12. 班规修缮：员工不碰 git（提交归舟/船主）；代码活必须用执行室真跑过；生命周期/记忆纪律补走廊/执行室/平台代记事实。

**本程明确不修（防盖教堂）**：记忆向量检索接入（记忆库刚开始积累，攒起来再接）；急停文件双轨（/panic 已是主路，无害）；旧展示代码瘦身（历史展示用）。**验证边界（诚实标注）**：py_compile/tsc/build/执行室与走廊自检全过；带模型的端到端（干活带记忆、成长落记忆、验收文件清单、插话去重）**未真机验**——烧钱路径，待船主重启办公室后用第一件真活验。

**下一步（等船主拍）**：第一件真活给她——从小酒需求（她的房间/状态层/分层时钟…）挑最小的一件，大厅一句人话交给公司真跑。

*本蓝图服务于《公司的真谛》。每改一行问那把尺：场所+规则（留）/ 替活人编排判断（拆还权）/ 能不能（权限焊平台）。*
