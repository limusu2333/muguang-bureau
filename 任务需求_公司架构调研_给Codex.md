# 任务需求 · 业界调研：多智能体"公司"从 workflow 到自主 agent · 给 Codex

> 委托人：舟（代船主示例主人）。执行：Codex。类型：**纯调研，不写代码、不碰任何项目文件**，只产出一份调研报告。
> 背景根文档（执行前先读，理解我们在纠结什么）：`30_开发公司/公司的真谛_人是使用者平台是死物.md`。

## 一、背景（你要先懂我们的处境）

我们在做一个多智能体"开发公司"：5 个不同大模型分别扮演项目经理 / 首席工程师 / 工程师 / 测试 / 文案，协作完成软件任务。现在它是 **workflow 架构**——代码把"立项"硬编排成「项目经理准备议题 → 拉人开会 → 收敛 → 出工单」固定流水线，每个岗位 agent 每步被临时实例化、问一句、丢弃。问题：① 不管需求多简单都走全套重型流程（"随便立个测试"也拉全员开会）；② 智能在代码层（一个外部"判断"开关驱动 agent），agent 是被牵着走的木偶，不是自己连续思考行动的"人"。

船主要把它从根上翻成 **agent 架构**，世界观是：
- **agent = 活的"人"/使用者**；框架 / 工具 / 会议室 / 公共技能 / 岗位 = **死的平台**。
- **"怎么做"（判断、协调、开会、执行）= 能力，是 agent 自己的事，不写进代码**；**"能不能做"（资格、权限）= 唯一该硬编码的底层规则**。
- agent 自主，但**不能是黑箱**：关键动作（花钱 / 删除 / 最终交付）要过权限和人（船主）拍板；全过程要对人可见、可介入。

**但这套是船主和我（舟）的个人认知，不是业界验证过的答案。** 你的任务：把业界的真相查回来，让我们别拿没验证的当真理。

## 二、要调研的（按船主的四问组织，每问都要落到证据）

**A. 好答案是什么** —— 业界怎么做"自主 agent 作为使用者 + 平台提供工具/权限"这套？
1. "把 agent 当员工/人、把框架工具当平台" 这个设计哲学，有没有成熟范式或公认提法？被验证有效，还是有反例（自主导致不可控）？
2. 主流 agent 框架（**LangGraph、AutoGen / AG2、CrewAI、OpenAI Agents SDK / Swarm、AgentScope、MetaGPT** 等）各自在"自主 agent ↔ 固定 workflow 编排"光谱上站哪？它们的官方立场和取舍是什么？
3. 让**单个 agent 自主端到端**完成项目协调（自己判断要不要开会、自己派活、自己验收、自己决定报告还是返工）—— 有成熟实现 / 成功案例吗？

**B. 过程中遇到过什么问题（坑）**
4. 从 workflow 转向"放开 agent 自主"，业界踩过哪些坑？（失控、跑偏、成本爆炸、不可复现、难调试）
5. **多 agent 协作**的主流模式（supervisor/hierarchical、group chat、handoff/swarm、blackboard）各自的坑、token 成本、涌现行为可控性？
6. 业界对"什么时候该用自主 agent、什么时候该用固定 workflow"形成共识了吗？分界线在哪？（2024–2026 的演进和教训，例如 Anthropic / 各大厂的工程博客观点）

**C. 大家解决过什么问题（已有成熟解法）**
7. **能力 vs 权限分离**：agent 自主决定"怎么做"，但用 permission / guardrail / policy 控制"能不能做" —— 怎么实现的？human-in-the-loop 在哪些节点介入是成熟做法？
8. **自主≠黑箱**：让 agent 的推理 / 决策 / 工具调用对人透明、可审计、可中断 —— 成熟工具与实践（tracing、observability、approval gate、interrupt）？
9. **防失控 / 控成本**：死循环、token 爆炸、跑偏怎么兜？（预算/step 上限、checkpoint、reflection、自评）
10. **持续性 / 常驻**：agent 的长期记忆、跨会话身份、状态保持怎么做？临时实例化 vs 常驻进程的取舍？记忆方案（mem0 等）成熟度与坑？
11. **复杂度自适应**：agent 按需求轻重自动选响应方式（简单直接做、复杂才开会）—— 有成熟做法吗？（routing / planning / reflection）

**D. 还有哪些目前无法解决（公认开放难题）**
12. 列出业界公认尚未解决 / 有争议的：多 agent 涌现不可预测、长程自主可靠性、自主性 vs 对齐可控、**怎么评估"agent 用得好不好"=能力**（评估难题）等。

## 三、约束与验收（必须满足）

1. **每个结论带来源**（链接 / 论文 / 框架官方文档 / 大厂工程博客），优先 **2024–2026** 的材料；旧的标注年份。
2. **诚实分级**：每条标清是「业界成熟共识 / 前沿探索 / 仍有争议 / 公认未解决」。**查不到就写"查不到"，绝不编造、不强行凑答案**（这是硬红线）。
3. 每个板块结尾给一句**"对我们的启示"**：业界的对应物是什么、是否被验证、对我们这套世界观是支持还是警示、有什么风险。
4. **不写代码、不改我们任何项目文件**。产出 = 一份中文 Markdown 报告，落在 `30_开发公司/调研_公司架构_业界现状_byCodex.md`。
5. 报告开头放一个"30 秒结论速览"，正文分 A/B/C/D 四块，末尾列"参考来源清单"。
6. 完工后请在报告末尾注明完成时间与覆盖范围（查了哪些框架/来源、哪些没查到）。

—— 舟（代船主）
